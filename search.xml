<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>STM32定时器与PID算法</title>
    <url>/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<h2 id="1-STM32定时器"><a href="#1-STM32定时器" class="headerlink" title="1. STM32定时器"></a>1. STM32定时器</h2><h3 id="1-1-定时器与定时中断（Timer）"><a href="#1-1-定时器与定时中断（Timer）" class="headerlink" title="1.1 定时器与定时中断（Timer）"></a>1.1 定时器与定时中断（Timer）</h3><h4 id="1-1-1-TIM简介"><a href="#1-1-1-TIM简介" class="headerlink" title="1.1.1 TIM简介"></a>1.1.1 TIM简介</h4><ul>
<li><strong>TIM（Timer）定时器</strong></li>
<li><strong>定时器可以对输入的时钟进行计数，并在计数值达到设定值时触发中断</strong></li>
<li><strong>16位计数器、预分频器、自动重装寄存器的时基单元，在72MHz计数时钟下可以实现最大59.65s的定时</strong></li>
<li><strong>不仅具备基本的定时中断功能，而且还包含内外时钟源选择、输入捕获、输出比较、编码器接口、主从触发模式等多种功能</strong></li>
<li><strong>根据复杂度和应用场景分为了高级定时器、通用定时器、基本定时器三种类型</strong></li>
</ul>
<p><strong>定时器类型</strong></p>
<table>
<thead>
<tr>
<th align="left">类型</th>
<th><strong>编号</strong></th>
<th><strong>总线</strong></th>
<th><strong>功能</strong></th>
</tr>
</thead>
<tbody><tr>
<td align="left">高级定时器</td>
<td>TIM1、TIM8</td>
<td>APB2</td>
<td>拥有通用定时器全部功能，并额外具有重复计数器、死区生成、互补输出、刹车输入等功能</td>
</tr>
<tr>
<td align="left">通用定时器</td>
<td>TIM2、TIM3、TIM4、TIM5</td>
<td>APB1</td>
<td>拥有基本定时器全部功能，并额外具有内外时钟源选择、输入捕获、输出比较、编码器接口、主从触发模式等功能</td>
</tr>
<tr>
<td align="left">基本定时器</td>
<td>TIM6、TIM7</td>
<td>APB1</td>
<td>拥有定时中断、主模式触发DAC的功能</td>
</tr>
</tbody></table>
<blockquote>
<p>不同的芯片拥有的定时器资源不同，例如STM32F103C8T6定时器资源：TIM1、TIM2、TIM3、TIM4</p>
</blockquote>
<h4 id="1-1-2-定时器框架"><a href="#1-1-2-定时器框架" class="headerlink" title="1.1.2 定时器框架"></a>1.1.2 定时器框架</h4><h5 id="1、高级定时器"><a href="#1、高级定时器" class="headerlink" title="1、高级定时器"></a>1、高级定时器</h5><p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1706255487441.png" alt="高级定时器框图"></p>
<h5 id="2、通用定时器"><a href="#2、通用定时器" class="headerlink" title="2、通用定时器"></a>2、通用定时器</h5><p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1706255081006.png" alt="通用定时器框图"></p>
<ul>
<li>通用定时器有多种时钟选择（外部时钟模式1、2，内部时钟）</li>
<li>定时器级联与主从模式相关。（参考：[1.3.4 主从触发模式](#1.3.4 主从触发模式)）</li>
</ul>
<h5 id="3、基本定时器"><a href="#3、基本定时器" class="headerlink" title="3、基本定时器"></a>3、基本定时器</h5><p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1706254347422.png" alt="基本定时器框图"></p>
<blockquote>
<p>⚠：–基本定时器仅支持向上计数–时钟选择也仅支持内部时钟，为72MHZ</p>
</blockquote>
<h4 id="1-1-3-定时中断基本结构"><a href="#1-1-3-定时中断基本结构" class="headerlink" title="1.1.3 定时中断基本结构"></a>1.1.3 定时中断基本结构</h4><p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1706255701792.png" alt="定时中断基本结果"></p>
<h4 id="1-1-4-时序图"><a href="#1-1-4-时序图" class="headerlink" title="1.1.4 时序图"></a>1.1.4 时序图</h4><p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1706256017333.png" alt="预分频器时序"></p>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1706256032654.png" alt="计数器时序"></p>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1706256043889.png" alt="计数器无预装时序"></p>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1706256059025.png" alt="计数器有预装时序"></p>
<h4 id="1-1-5-RCC时钟树"><a href="#1-1-5-RCC时钟树" class="headerlink" title="1.1.5 RCC时钟树"></a>1.1.5 RCC时钟树</h4><p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1706255928324.png" alt="时钟树"></p>
<h4 id="1-1-6-代码示例"><a href="#1-1-6-代码示例" class="headerlink" title="1.1.6 代码示例"></a>1.1.6 代码示例</h4><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"stm32f10x.h"</span>                  <span class="hljs-comment">// Device header</span></span>

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：定时中断初始化</span>
<span class="hljs-comment">  * 参    数：无</span>
<span class="hljs-comment">  * 返 回 值：无</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">Timer_Init</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-comment">/*开启时钟*/</span>
 RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM2, ENABLE);			<span class="hljs-comment">//开启TIM2的时钟</span>
 
 <span class="hljs-comment">/*配置时钟源*/</span>
 TIM_InternalClockConfig(TIM2);		<span class="hljs-comment">//选择TIM2为内部时钟，若不调用此函数，TIM默认也为内部时钟</span>
 
 <span class="hljs-comment">/*时基单元初始化*/</span>
 TIM_TimeBaseInitTypeDef TIM_TimeBaseInitStructure;				<span class="hljs-comment">//定义结构体变量</span>
 TIM_TimeBaseInitStructure.TIM_ClockDivision = TIM_CKD_DIV1;		<span class="hljs-comment">//时钟分频，选择不分频</span>
 TIM_TimeBaseInitStructure.TIM_CounterMode = TIM_CounterMode_Up;<span class="hljs-comment">//计数器模式，选择向上计数</span>
 TIM_TimeBaseInitStructure.TIM_Period = <span class="hljs-number">10000</span> - <span class="hljs-number">1</span>;				<span class="hljs-comment">//计数周期，即ARR的值</span>
 TIM_TimeBaseInitStructure.TIM_Prescaler = <span class="hljs-number">7200</span> - <span class="hljs-number">1</span>;				<span class="hljs-comment">//预分频器，即PSC的值</span>
 TIM_TimeBaseInitStructure.TIM_RepetitionCounter = <span class="hljs-number">0</span>;	<span class="hljs-comment">//重复计数器，高级定时器才会用到</span>
 TIM_TimeBaseInit(TIM2, &amp;TIM_TimeBaseInitStructure);				
 
 <span class="hljs-comment">/*中断输出配置*/</span>
 TIM_ClearFlag(TIM2, TIM_FLAG_Update);						
    <span class="hljs-comment">/*清除定时器更新标志位TIM_TimeBaseInit函数末尾，</span>
<span class="hljs-comment">    手动产生了更新事件若不清除此标志位，则开启中断后，</span>
<span class="hljs-comment">    会立刻进入一次中断如果不介意此问题，则不清除此标志位也可*/</span>
 
 TIM_ITConfig(TIM2, TIM_IT_Update, ENABLE);					<span class="hljs-comment">//开启TIM2的更新中断</span>
 
 <span class="hljs-comment">/*NVIC中断分组*/</span>
 NVIC_PriorityGroupConfig(NVIC_PriorityGroup_2);				<span class="hljs-comment">//配置NVIC为分组2</span>
 <span class="hljs-comment">/*即抢占优先级范围：0~3，响应优先级范围：0~3</span>
<span class="hljs-comment"> 此分组配置在整个工程中仅需调用一次</span>
<span class="hljs-comment"> 若有多个中断，可以把此代码放在main函数内，while循环之前</span>
<span class="hljs-comment"> 若调用多次配置分组的代码，则后执行的配置会覆盖先执行的配置*/</span>
 
 <span class="hljs-comment">/*NVIC配置*/</span>
 NVIC_InitTypeDef NVIC_InitStructure;						<span class="hljs-comment">//定义结构体变量</span>
 NVIC_InitStructure.NVIC_IRQChannel = TIM2_IRQn;				<span class="hljs-comment">//选择配置NVIC的TIM2线</span>
 NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;				<span class="hljs-comment">//指定NVIC线路使能</span>
 NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority = <span class="hljs-number">2</span>;	<span class="hljs-comment">//指定NVIC线路的抢占优先级为2</span>
 NVIC_InitStructure.NVIC_IRQChannelSubPriority = <span class="hljs-number">1</span>;			<span class="hljs-comment">//指定NVIC线路的响应优先级为1</span>
 NVIC_Init(&amp;NVIC_InitStructure);							
 
 <span class="hljs-comment">/*TIM使能*/</span>
 TIM_Cmd(TIM2, ENABLE);			<span class="hljs-comment">//使能TIM2，定时器开始运行</span>
}

<span class="hljs-comment">/* 定时器中断函数，可以复制到使用它的地方</span>
<span class="hljs-comment">void TIM2_IRQHandler(void)</span>
<span class="hljs-comment">{</span>
<span class="hljs-comment"> if (TIM_GetITStatus(TIM2, TIM_IT_Update) == SET)</span>
<span class="hljs-comment"> {</span>
<span class="hljs-comment">  </span>
<span class="hljs-comment">  TIM_ClearITPendingBit(TIM2, TIM_IT_Update);</span>
<span class="hljs-comment"> }</span>
<span class="hljs-comment">}</span>
<span class="hljs-comment">*/</span>
</code></pre>



<h3 id="1-2-输出比较（Output-Compare）"><a href="#1-2-输出比较（Output-Compare）" class="headerlink" title="1.2 输出比较（Output Compare）"></a>1.2 输出比较（Output Compare）</h3><h4 id="1-2-1-输出比较介绍"><a href="#1-2-1-输出比较介绍" class="headerlink" title="1.2.1 输出比较介绍"></a>1.2.1 输出比较介绍</h4><ul>
<li><strong>OC（Output Compare）输出比较</strong></li>
<li><strong>输出比较可以通过比较CNT与CCR寄存器值的关系，来对输出电平进行置1、置0或翻转的操作，用于输出一定频率和占空比的PWM波形</strong></li>
<li><strong>每个高级定时器和通用定时器都拥有4个输出比较通道</strong></li>
<li><strong>高级定时器的前3个通道额外拥有死区生成和互补输出的功能</strong>（用于驱动三相无刷电机）</li>
</ul>
<h4 id="1-2-2-PWM简介"><a href="#1-2-2-PWM简介" class="headerlink" title="1.2.2 PWM简介"></a>1.2.2 PWM简介</h4><ul>
<li><p>PWM（Pulse Width Modulation）脉冲宽度调制</p>
</li>
<li><p>在具有惯性的系统中，可以通过对一系列脉冲的宽度进行调制，来等效地获得所需要的模拟参量，常应用于电机控速等领域</p>
</li>
<li><p>PWM参数：</p>
<p>​      ⭐频率 = 1 / T_S            ⭐占空比 = T_ON / T_S           分辨率 = 占空比变化步距</p>
</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1706197859960.png" alt="PWM"></p>
<h4 id="1-2-3-输出比较通道"><a href="#1-2-3-输出比较通道" class="headerlink" title="1.2.3 输出比较通道"></a>1.2.3 输出比较通道</h4><p><strong>高级定时器输出比较通道：</strong></p>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1706198238483.png" alt="高级定时器输出比较通道"></p>
<p><strong>高级定时器输出比较通道：</strong></p>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1706199429609.png" alt="高级定时器输出比较通道"></p>
<blockquote>
<p>关于定时器主模式控制器参考[1.3.4 主从触发模式](#1.3.4 主从触发模式)</p>
</blockquote>
<h4 id="1-2-4-输出比较模式"><a href="#1-2-4-输出比较模式" class="headerlink" title="1.2.4 输出比较模式"></a>1.2.4 输出比较模式</h4><table>
<thead>
<tr>
<th align="left"><strong>模式</strong></th>
<th><strong>描述</strong></th>
</tr>
</thead>
<tbody><tr>
<td align="left">冻结</td>
<td>CNT=CCR时，REF保持为原状态</td>
</tr>
<tr>
<td align="left">匹配时置有效电平</td>
<td>CNT=CCR时，REF置有效电平</td>
</tr>
<tr>
<td align="left">匹配时置无效电平</td>
<td>CNT=CCR时，REF置无效电平</td>
</tr>
<tr>
<td align="left">匹配时电平翻转</td>
<td>CNT=CCR时，REF电平翻转</td>
</tr>
<tr>
<td align="left">强制为无效电平</td>
<td>CNT与CCR无效，REF强制为无效电平</td>
</tr>
<tr>
<td align="left">强制为有效电平</td>
<td>CNT与CCR无效，REF强制为有效电平</td>
</tr>
<tr>
<td align="left">PWM模式1</td>
<td>向上计数：CNT&lt;CCR时，REF置有效电平，CNT≥CCR时，REF置无效电平   向下计数：CNT&gt;CCR时，REF置无效电平，CNT≤CCR时，REF置有效电平</td>
</tr>
<tr>
<td align="left">PWM模式2</td>
<td>向上计数：CNT&lt;CCR时，REF置无效电平，CNT≥CCR时，REF置有效电平   向下计数：CNT&gt;CCR时，REF置有效电平，CNT≤CCR时，REF置无效电平</td>
</tr>
</tbody></table>
<h4 id="1-2-5-PWM基本结构"><a href="#1-2-5-PWM基本结构" class="headerlink" title="1.2.5 PWM基本结构"></a>1.2.5 PWM基本结构</h4><p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1706200055360.png" alt="PWM基本结构"></p>
<ul>
<li>PWM频率：  Freq = CK_PSC / (PSC + 1) / (ARR + 1)</li>
<li>PWM占空比：  Duty = CCR / (ARR + 1)</li>
<li>PWM分辨率： Reso = 1 / (ARR+ 1)</li>
</ul>
<blockquote>
<p>关于参数介绍—</p>
<p>CK_PSC–时钟频率：APB1为36MHZ，APB2为72MHZ</p>
<p>PSC –预分频值：代码设置</p>
<p>ARR –自动重装值：代码设置</p>
<p>CNT–定时器计数值：按时钟频率计数</p>
</blockquote>
<h4 id="1-2-6-硬件介绍（舵机、TB6612芯片）"><a href="#1-2-6-硬件介绍（舵机、TB6612芯片）" class="headerlink" title="1.2.6 硬件介绍（舵机、TB6612芯片）"></a>1.2.6 硬件介绍（舵机、TB6612芯片）</h4><ul>
<li><strong>舵机</strong></li>
</ul>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1706200565288.png" alt="舵机"></p>
<ul>
<li><strong>TB6612直流电机驱动</strong></li>
</ul>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1706200648810.png" alt="TB6612"></p>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1706200615939.png" alt="硬件电路"></p>
<h4 id="1-2-7-代码示例"><a href="#1-2-7-代码示例" class="headerlink" title="1.2.7 代码示例"></a>1.2.7 代码示例</h4><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"stm32f10x.h"</span>                  <span class="hljs-comment">// Device header</span></span>

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：PWM初始化</span>
<span class="hljs-comment">  * 参    数：无</span>
<span class="hljs-comment">  * 返 回 值：无</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">PWM_Init</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-comment">/*开启时钟*/</span>
 RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM2, ENABLE);			<span class="hljs-comment">//开启TIM2的时钟</span>
 RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA, ENABLE);			<span class="hljs-comment">//开启GPIOA的时钟</span>
 
 <span class="hljs-comment">/*GPIO初始化*/</span>
 GPIO_InitTypeDef GPIO_InitStructure;
 GPIO_InitStructure.GPIO_Mode = GPIO_Mode_AF_PP;
 GPIO_InitStructure.GPIO_Pin = GPIO_Pin_1;
 GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
 GPIO_Init(GPIOA, &amp;GPIO_InitStructure);							<span class="hljs-comment">//将PA1引脚初始化为复用推挽输出	</span>
                 <span class="hljs-comment">//受外设控制的引脚，均需要配置为复用模式</span>
 
 <span class="hljs-comment">/*配置时钟源*/</span>
 TIM_InternalClockConfig(TIM2);		<span class="hljs-comment">//选择TIM2为内部时钟，若不调用此函数，TIM默认也为内部时钟</span>
 
 <span class="hljs-comment">/*时基单元初始化*/</span>
 TIM_TimeBaseInitTypeDef TIM_TimeBaseInitStructure;				<span class="hljs-comment">//定义结构体变量</span>
 TIM_TimeBaseInitStructure.TIM_ClockDivision = TIM_CKD_DIV1;     <span class="hljs-comment">//时钟分频，选择不分频</span>
 TIM_TimeBaseInitStructure.TIM_CounterMode = TIM_CounterMode_Up; <span class="hljs-comment">//选择向上计数</span>
 TIM_TimeBaseInitStructure.TIM_Period = <span class="hljs-number">20000</span> - <span class="hljs-number">1</span>;				<span class="hljs-comment">//计数周期，即ARR的值</span>
 TIM_TimeBaseInitStructure.TIM_Prescaler = <span class="hljs-number">72</span> - <span class="hljs-number">1</span>;				<span class="hljs-comment">//预分频器，即PSC的值</span>
 TIM_TimeBaseInitStructure.TIM_RepetitionCounter = <span class="hljs-number">0</span>;      <span class="hljs-comment">//重复计数器，高级定时器才会用到</span>
 TIM_TimeBaseInit(TIM2, &amp;TIM_TimeBaseInitStructure);          
 
 <span class="hljs-comment">/*输出比较初始化*/</span> 
 TIM_OCInitTypeDef TIM_OCInitStructure;					  <span class="hljs-comment">//定义结构体变量</span>
 TIM_OCStructInit(&amp;TIM_OCInitStructure);                   <span class="hljs-comment">//给结构体所有成员都赋一个默认值 </span>
                                                        
 TIM_OCInitStructure.TIM_OCMode = TIM_OCMode_PWM1;            <span class="hljs-comment">//输出比较模式，选择PWM模式1</span>
 TIM_OCInitStructure.TIM_OCPolarity = TIM_OCPolarity_High;    <span class="hljs-comment">//输出极性，选择为高，若选择极性为低，则输出高低电平取反</span>
 TIM_OCInitStructure.TIM_OutputState = TIM_OutputState_Enable;   <span class="hljs-comment">//输出使能</span>
 TIM_OCInitStructure.TIM_Pulse = <span class="hljs-number">0</span>;								<span class="hljs-comment">//初始的CCR比较值</span>
 TIM_OC2Init(TIM2, &amp;TIM_OCInitStructure);                        <span class="hljs-comment">//配置输出比较通道2</span>
 
 <span class="hljs-comment">/*TIM使能*/</span>
 TIM_Cmd(TIM2, ENABLE);			<span class="hljs-comment">//使能TIM2，定时器开始运行</span>
}

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：PWM设置CCR</span>
<span class="hljs-comment">  * 参    数：Compare 要写入的CCR的值，范围：0~100</span>
<span class="hljs-comment">  * 返 回 值：无</span>
<span class="hljs-comment">  * 注意事项：CCR和ARR共同决定占空比，此函数仅设置CCR的值，并不直接是占空比</span>
<span class="hljs-comment">  *           占空比Duty = CCR / (ARR + 1)</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">PWM_SetCompare2</span><span class="hljs-params">(<span class="hljs-type">uint16_t</span> Compare)</span>
{
 TIM_SetCompare2(TIM2, Compare);		<span class="hljs-comment">//设置CCR的值</span>
}
</code></pre>



<h3 id="1-3-输入捕获（Input-Capture）"><a href="#1-3-输入捕获（Input-Capture）" class="headerlink" title="1.3 输入捕获（Input Capture）"></a>1.3 输入捕获（Input Capture）</h3><h4 id="1-3-1-输入捕获介绍"><a href="#1-3-1-输入捕获介绍" class="headerlink" title="1.3.1 输入捕获介绍"></a>1.3.1 输入捕获介绍</h4><ul>
<li><strong>IC（Input Capture）输入捕获</strong></li>
<li><strong>输入捕获模式下，当通道输入引脚出现指定电平跳变时，当前CNT的值将被锁存到CCR中，可用于测量PWM波形的频率、占空比、脉冲间隔、电平持续时间等参数</strong></li>
<li><strong>每个高级定时器和通用定时器都拥有4个输入捕获通道</strong></li>
<li><strong>可配置为PWMI模式，同时测量频率和占空比</strong></li>
<li><strong>可配合主从触发模式，实现硬件全自动测量</strong></li>
</ul>
<h4 id="1-3-2-频率测量"><a href="#1-3-2-频率测量" class="headerlink" title="1.3.2 频率测量"></a>1.3.2 频率测量</h4><p>频率测量分为测频法和测周法：</p>
<ul>
<li>测频法—一定闸门时间T内，记上升沿的次数$$N$$，取周期的倒数得可到频率：$$f_x=N/T$$</li>
<li>测周法—在两个上升沿之间，以标准频率$$f_c$$计次$$N$$，标准频率的周期为$$1/f_c$$,到周期T为$$N/f_c$$，取周期的倒数即可得到频率：$$f_x=f_c/N$$</li>
<li>中界频率—令$$f_x=N/T$$和$$f_x=f_c/N$$中的$$N$$相等解出$$f_x$$ 得到测频法与测周法误差相等的频率点：$$f_m=\sqrt(f_c/T)$$</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1705737398459.png" alt="频率测量方法"></p>
<h4 id="1-3-3-输入捕获框架"><a href="#1-3-3-输入捕获框架" class="headerlink" title="1.3.3 输入捕获框架"></a>1.3.3 输入捕获框架</h4><p>下面是通用定时器的框架：</p>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1705743432415.png" alt="通用定时器框架"></p>
<ul>
<li><strong>工作流程：</strong>经过输入滤波器和极性选择进入预分频器，可以选择进行触发信号分频，然后触发信号进入捕获电路工作，每来一个触发信号，CNT的值向CCR转运一次，同时触发捕获事件，在状态寄存器置标志位触发中断，可以设置中断处理。</li>
<li><strong>测周法实现：</strong>以上升沿为触发信号，CNT计数器由内部标准时钟驱动（驱动时钟为$$f_c$$ ），CNT数值即可记录两个上升沿的事件间隔，即为周期，取倒数可获得测周法的频率。</li>
</ul>
<blockquote>
<p>每次捕获以后要将CNT清零，可以用主从触发模式自动完成。</p>
</blockquote>
<h4 id="1-3-4-主从触发模式"><a href="#1-3-4-主从触发模式" class="headerlink" title="1.3.4 主从触发模式"></a>1.3.4 主从触发模式</h4><ul>
<li>主模式：将定时器内部的信号映射到TRGO引脚，用于触发别的外设。</li>
<li>从模式：接收其他外设或自身外设的一些信号用于控制自身定时器的运行。**(被其他信号控制)**</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1705746225685.png" alt="主从触发模式"></p>
<p><strong>从模式的触发源选择：</strong></p>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1705745717717.png" alt="从模式的触发源"></p>
<p><strong>通用定时器内部触发的连接：</strong></p>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1705746412968.png" alt="通用定时器内部触发的连接"></p>
<blockquote>
<p>高级定时器的内部触发连接需要自己查stm32开发手册。</p>
</blockquote>
<p><strong>从模式选择(与上表的顺序一致):</strong></p>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1705745521282.png" alt="从模式选择"></p>
<h4 id="1-3-5-输入捕获基本结构"><a href="#1-3-5-输入捕获基本结构" class="headerlink" title="1.3.5 输入捕获基本结构"></a>1.3.5 输入捕获基本结构</h4><p>测周法读取频率</p>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1705747154895.png" alt="输入捕获基本结构"></p>
<h4 id="1-3-6-PWMI模式"><a href="#1-3-6-PWMI模式" class="headerlink" title="1.3.6 PWMI模式"></a>1.3.6 PWMI模式</h4><p>同时读取频率和占空比</p>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1705747401629.png" alt="PWMI模式"></p>
<h4 id="1-3-7-代码示例"><a href="#1-3-7-代码示例" class="headerlink" title="1.3.7 代码示例"></a>1.3.7 代码示例</h4><pre><code class="hljs c"><span class="hljs-type">void</span> <span class="hljs-title function_">IC_Init</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-comment">/*开启时钟*/</span>
 RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM3, ENABLE);			<span class="hljs-comment">//开启TIM3的时钟</span>
 RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA, ENABLE);			<span class="hljs-comment">//开启GPIOA的时钟</span>
 
 <span class="hljs-comment">/*GPIO初始化*/</span>
 GPIO_InitTypeDef GPIO_InitStructure;
 GPIO_InitStructure.GPIO_Mode = GPIO_Mode_IPU;
 GPIO_InitStructure.GPIO_Pin = GPIO_Pin_6;
 GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
 GPIO_Init(GPIOA, &amp;GPIO_InitStructure);						<span class="hljs-comment">//将PA6引脚初始化为上拉输入</span>
 
 <span class="hljs-comment">/*配置时钟源*/</span>
 TIM_InternalClockConfig(TIM3);		<span class="hljs-comment">//选择TIM3为内部时钟，若不调用此函数，TIM默认也为内部时钟</span>
 
 <span class="hljs-comment">/*时基单元初始化*/</span>
 TIM_TimeBaseInitTypeDef TIM_TimeBaseInitStructure;				<span class="hljs-comment">//定义结构体变量</span>
 TIM_TimeBaseInitStructure.TIM_ClockDivision = TIM_CKD_DIV1;     <span class="hljs-comment">//时钟分频，选择不分频</span>
 TIM_TimeBaseInitStructure.TIM_CounterMode = TIM_CounterMode_Up; <span class="hljs-comment">//计数器模式，向上计数</span>
 TIM_TimeBaseInitStructure.TIM_Period = <span class="hljs-number">65536</span> - <span class="hljs-number">1</span>;               <span class="hljs-comment">//计数周期，即ARR的值</span>
 TIM_TimeBaseInitStructure.TIM_Prescaler = <span class="hljs-number">72</span> - <span class="hljs-number">1</span>;               <span class="hljs-comment">//预分频器，即PSC的值</span>
 TIM_TimeBaseInitStructure.TIM_RepetitionCounter = <span class="hljs-number">0</span>;      <span class="hljs-comment">//重复计数器，高级定时器才会用到</span>
 TIM_TimeBaseInit(TIM3, &amp;TIM_TimeBaseInitStructure);             
 
 <span class="hljs-comment">/*PWMI模式初始化*/</span>
 TIM_ICInitTypeDef TIM_ICInitStructure;							<span class="hljs-comment">//定义结构体变量</span>
 TIM_ICInitStructure.TIM_Channel = TIM_Channel_1;				<span class="hljs-comment">//选择配置定时器通道1</span>
 TIM_ICInitStructure.TIM_ICFilter = <span class="hljs-number">0xF</span>;					<span class="hljs-comment">//输入滤波器参数，可以过滤信号抖动</span>
 TIM_ICInitStructure.TIM_ICPolarity = TIM_ICPolarity_Rising;  <span class="hljs-comment">//极性，选择为上升沿触发捕获</span>
 <span class="hljs-comment">//捕获预分频，选择不分频，每次信号都触发捕获</span>
    TIM_ICInitStructure.TIM_ICPrescaler = TIM_ICPSC_DIV1; 
    <span class="hljs-comment">//输入信号交叉，选择直通，不交叉</span>
 TIM_ICInitStructure.TIM_ICSelection = TIM_ICSelection_DirectTI;	
 TIM_PWMIConfig(TIM3, &amp;TIM_ICInitStructure);						
 <span class="hljs-comment">//此函数同时会把另一个通道配置为相反的配置，实现PWMI模式</span>

 <span class="hljs-comment">/*选择触发源及从模式*/</span>
 TIM_SelectInputTrigger(TIM3, TIM_TS_TI1FP1);		<span class="hljs-comment">//触发源选择TI1FP1</span>
 TIM_SelectSlaveMode(TIM3, TIM_SlaveMode_Reset);		<span class="hljs-comment">//从模式选择复位</span>
              <span class="hljs-comment">//即TI1产生上升沿时，会触发CNT归零</span>
 
 <span class="hljs-comment">/*TIM使能*/</span>
 TIM_Cmd(TIM3, ENABLE);			<span class="hljs-comment">//使能TIM3，定时器开始运行</span>
}
<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：获取输入捕获的频率</span>
<span class="hljs-comment">  * 参    数：无</span>
<span class="hljs-comment">  * 返 回 值：捕获得到的频率</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">uint32_t</span> <span class="hljs-title function_">IC_GetFreq</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-keyword">return</span> <span class="hljs-number">1000000</span> / (TIM_GetCapture1(TIM3) + <span class="hljs-number">1</span>);		<span class="hljs-comment">//测周法得到频率fx = fc / N，这里不执行+1的操作也可</span>
}

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：获取输入捕获的占空比</span>
<span class="hljs-comment">  * 参    数：无</span>
<span class="hljs-comment">  * 返 回 值：捕获得到的占空比</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">uint32_t</span> <span class="hljs-title function_">IC_GetDuty</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-keyword">return</span> (TIM_GetCapture2(TIM3) + <span class="hljs-number">1</span>) * <span class="hljs-number">100</span> / (TIM_GetCapture1(TIM3) + <span class="hljs-number">1</span>);	<span class="hljs-comment">//占空比Duty = CCR2 / CCR1 * 100，这里不执行+1的操作也可</span>
}
</code></pre>

<h3 id="1-4-编码器接口（Encoder）"><a href="#1-4-编码器接口（Encoder）" class="headerlink" title="1.4 编码器接口（Encoder）"></a>1.4 编码器接口（Encoder）</h3><h4 id="1-4-1-编码器接口介绍"><a href="#1-4-1-编码器接口介绍" class="headerlink" title="1.4.1 编码器接口介绍"></a>1.4.1 编码器接口介绍</h4><ul>
<li><strong>（Encoder Interface ）编码器接口</strong></li>
<li><strong>编码器接口可接收增量（正交）编码器的信号，根据编码器旋转产生的正交信号脉冲，自动控制CNT自增或自减，从而指示编码器的位置、旋转方向和旋转速度</strong></li>
<li><strong>每个高级定时器和通用定时器都拥有1个编码器接口（用于编码器接口的定时器无法再做其他事）</strong></li>
<li><strong>定时器的通道1和通道2作为编码器接口</strong></li>
</ul>
<p>如下图，为正交编码器（A相与B相相差90°），通过观察A相高低电平时B相的电平判断正反转，同时脉冲频率即体现电机的转速。</p>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1705690112223.png" alt="正交编码器"></p>
<h4 id="1-4-2-编码器接口基本结构"><a href="#1-4-2-编码器接口基本结构" class="headerlink" title="1.4.2 编码器接口基本结构"></a>1.4.2 编码器接口基本结构</h4><p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1705734851593.png" alt="编码器接口基本结构"></p>
<p>说明一下极性选择：</p>
<ul>
<li><p>选择“上升沿”则正常波形</p>
</li>
<li><p>选择“下降沿”则需要经过一个“非门”（逻辑）</p>
<blockquote>
<p>详情可看[1.4.5 计数示例（极性选择）](#1.4.5 计数示例（极性选择）)</p>
</blockquote>
</li>
</ul>
<h4 id="1-4-3-编码器接口工作模式"><a href="#1-4-3-编码器接口工作模式" class="headerlink" title="1.4.3 编码器接口工作模式"></a>1.4.3 编码器接口工作模式</h4><p>编码器接口工作模式分三种：</p>
<ul>
<li>仅在A相上升沿和下降沿计数</li>
<li>仅在B相上升沿和下降沿计数</li>
<li>在AB相都计数（精度最高，一些资料会称为四倍频）</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1705735207132.png" alt="计数方式"></p>
<blockquote>
<p>工作模式表格要对应**[1.4.1编码器接口介绍](#1.4.1 编码器接口介绍)**的表格看，即通过看A相B相对应电平判断正反转。</p>
</blockquote>
<h4 id="1-4-4-四倍频法"><a href="#1-4-4-四倍频法" class="headerlink" title="1.4.4 #四倍频法"></a>1.4.4 #四倍频法</h4><p>四倍频是通过程序提升我们的编码器的精度的一种数据处理方法， 可以有效的最大化我们的编码器的精度和测量精度。 </p>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1705690390565.png" alt="四倍频法"></p>
<p>上图是一个我们编码器输出的波形图， 正常我们一般的处理方式是通过 A 相去计数， B 相去判断目前的转动方向。 具体实现比如： A 相的上升沿计数或者下降沿计数， 同时在 A 相的上升沿或者下降沿来根据 B 相此时的电平状态来判断转向。四倍频则是同时计算 AB 两相的每个跳边沿， 这样子原本在 A 相计数的一个脉冲周期内就实现了四次计数， 从而实现了精度的提升。 </p>
<h4 id="1-4-5-计数示例（极性选择）"><a href="#1-4-5-计数示例（极性选择）" class="headerlink" title="1.4.5 计数示例（极性选择）"></a>1.4.5 计数示例（极性选择）</h4><ul>
<li>正常不反相，正转自增，反转自减。</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1705736375374.png" alt="计数模式"></p>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1705736385998.png" alt="实际波形"></p>
<ul>
<li>TI1极性反转（需要将TI1的<strong>高低电平反转</strong>，在对应表格找正反转）</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1705736505835.png" alt="计数模式"></p>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1705736511255.png" alt="实际波形"></p>
<h4 id="1-4-6-代码示例"><a href="#1-4-6-代码示例" class="headerlink" title="1.4.6 代码示例"></a>1.4.6 代码示例</h4><pre><code class="hljs c"><span class="hljs-type">void</span> <span class="hljs-title function_">Encoder_Init</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-comment">/*开启时钟*/</span>
 RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM3, ENABLE);			<span class="hljs-comment">//开启TIM3的时钟</span>
 RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA, ENABLE);			<span class="hljs-comment">//开启GPIOA的时钟</span>
 
 <span class="hljs-comment">/*GPIO初始化*/</span>
 GPIO_InitTypeDef GPIO_InitStructure;
 GPIO_InitStructure.GPIO_Mode = GPIO_Mode_IPU;
 GPIO_InitStructure.GPIO_Pin = GPIO_Pin_6 | GPIO_Pin_7;
 GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
 GPIO_Init(GPIOA, &amp;GPIO_InitStructure);				<span class="hljs-comment">//将PA6和PA7引脚初始化为上拉输入</span>
 
 <span class="hljs-comment">/*时基单元初始化*/</span>
 TIM_TimeBaseInitTypeDef TIM_TimeBaseInitStructure;			   <span class="hljs-comment">//定义结构体变量</span>
    <span class="hljs-comment">//时钟分频，选择不分频，此参数用于配置滤波器时钟，不影响时基单元功能</span>
 TIM_TimeBaseInitStructure.TIM_ClockDivision = TIM_CKD_DIV1;    
 TIM_TimeBaseInitStructure.TIM_CounterMode = TIM_CounterMode_Up;<span class="hljs-comment">//计数器模式，选择向上计数</span>
 TIM_TimeBaseInitStructure.TIM_Period = <span class="hljs-number">65536</span> - <span class="hljs-number">1</span>;           <span class="hljs-comment">//计数周期，即ARR的值</span>
 TIM_TimeBaseInitStructure.TIM_Prescaler = <span class="hljs-number">1</span> - <span class="hljs-number">1</span>;          <span class="hljs-comment">//预分频器，即PSC的值</span>
 TIM_TimeBaseInitStructure.TIM_RepetitionCounter = <span class="hljs-number">0</span>;    <span class="hljs-comment">//重复计数器，高级定时器才会用到</span>
 TIM_TimeBaseInit(TIM3, &amp;TIM_TimeBaseInitStructure);      
 
 <span class="hljs-comment">/*输入捕获初始化*/</span>
 TIM_ICInitTypeDef TIM_ICInitStructure;				<span class="hljs-comment">//定义结构体变量</span>
    <span class="hljs-comment">/*结构体初始化，若结构体没有完整赋值</span>
<span class="hljs-comment">    则最好执行此函数，给结构体所有成员都赋一个默认值</span>
<span class="hljs-comment">    避免结构体初值不确定的问题*/</span>
 TIM_ICStructInit(&amp;TIM_ICInitStructure);

 TIM_ICInitStructure.TIM_Channel = TIM_Channel_1;	<span class="hljs-comment">//选择配置定时器通道1</span>
 TIM_ICInitStructure.TIM_ICFilter = <span class="hljs-number">0xF</span>;				<span class="hljs-comment">//输入滤波器参数，可以过滤信号抖动</span>
 TIM_ICInit(TIM3, &amp;TIM_ICInitStructure);			
    
 TIM_ICInitStructure.TIM_Channel = TIM_Channel_2;	<span class="hljs-comment">//选择配置定时器通道2</span>
 TIM_ICInitStructure.TIM_ICFilter = <span class="hljs-number">0xF</span>;				<span class="hljs-comment">//输入滤波器参数，可以过滤信号抖动</span>
 TIM_ICInit(TIM3, &amp;TIM_ICInitStructure);							
 
 <span class="hljs-comment">/*编码器接口配置*/</span>
    <span class="hljs-comment">/*配置编码器模式以及两个输入通道是否反相</span>
<span class="hljs-comment"> 注意此时参数的Rising和Falling已经不代表上升沿和下降沿了，而是代表是否反相</span>
<span class="hljs-comment"> 此函数必须在输入捕获初始化之后进行，否则输入捕获的配置会覆盖此函数的部分配置*/</span>
 TIM_EncoderInterfaceConfig(TIM3, TIM_EncoderMode_TI12, TIM_ICPolarity_Rising, TIM_ICPolarity_Rising);

 <span class="hljs-comment">/*TIM使能*/</span>
 TIM_Cmd(TIM3, ENABLE);			<span class="hljs-comment">//使能TIM3，定时器开始运行</span>
}
<span class="hljs-type">int16_t</span> <span class="hljs-title function_">Encoder_Get_</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-type">int16_t</span> Temp;
 Temp = TIM_GetCounter(TIM3);
 TIM_SetCounter(TIM3, <span class="hljs-number">0</span>);
 <span class="hljs-keyword">return</span> Temp;
}
</code></pre>

<h2 id="2-PID控制算法"><a href="#2-PID控制算法" class="headerlink" title="2.PID控制算法"></a>2.PID控制算法</h2><h3 id="2-1-PID算法简介"><a href="#2-1-PID算法简介" class="headerlink" title="2.1 PID算法简介"></a>2.1 PID算法简介</h3><blockquote>
<p>PID 控制器以各种形式使用超过了 1 世纪，广泛应用在机械设备、气动设备 和电子设备.在工业应用中PID及其衍生算法是应用最广泛的算法之一，是当之无愧的万能算法</p>
</blockquote>
<p>PID 实指“比例 proportional”、“积分 integral”、“微分 derivative”，这三项构 成 PID 基本要素。每一项完成不同任务，对系统功能产生不同的影响。它的结构简单，参数易 于调整，是控制系统中经常采用的控制算法。</p>
<p><strong>PID：比例单元（P）、积分单元（I）和微分单元（D）组成</strong></p>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1707463945385.png" alt="PID算法"></p>
<p><strong>PID控制公式：</strong></p>
<p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1707463962759.png" alt="PID控制公式"></p>
<p>其中：**u(t)**为控制器输出的控制量；（输出）</p>
<p>**e(t)**为偏差信号，它等于给定量与输出量之差；（输入）</p>
<p><strong>KP</strong> 为比例系数；（对应参数 P）</p>
<p><strong>TI</strong> 为积分时间常数；（对应参数I）</p>
<p><strong>TD</strong> 为微分时间常数。(对应参数 D) </p>
<p>数字 PID 控制算法通常分为位置式 PID 控制算法和增量式 PID 控制算法。 </p>
<h4 id="2-1-1-位置式-PID-算法"><a href="#2-1-1-位置式-PID-算法" class="headerlink" title="2.1.1 位置式 PID 算法"></a>2.1.1 位置式 PID 算法</h4><p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1707463978757.png" alt="位置式PID"></p>
<p>e(k): <strong>用户设定的值（目标值） -  控制对象的当前的状态值</strong> </p>
<p><strong>比例P :</strong>    e(k)</p>
<p><strong>积分I :</strong>   ∑e(i)     误差的累加</p>
<p><strong>微分D :</strong>  e(k) - e(k-1)  这次误差-上次误差</p>
<p>也就是位置式PID是当前系统的实际位置，与你想要达到的预期位置的偏差，进行PID控制</p>
<p>因为有误差积分 ∑e(i)，一直累加，也就是当前的输出u(k)与过去的所有状态都有关系，用到了误差的累加值；（误差e会有误差累加），输出的u(k)对应的是执行机构的实际位置，，一旦控制输出出错(控制对象的当前的状态值出现问题 )，u(k)的大幅变化会引起系统的大幅变化</p>
<p>并且位置式PID在积分项达到饱和时,误差仍然会在积分作用下继续累积，一旦误差开始反向变化，系统需要一定时间从饱和区退出，所以在u(k)达到最大和最小时，要停止积分作用，并且要有积分限幅和输出限幅</p>
<p>所以在使用位置式PID时，一般我们直接使用PD控制</p>
<p>而位置式 PID 适用于执行机构不带积分部件的对象，如舵机和平衡小车的直立和温控系统的控制</p>
<h4 id="2-1-2-增量式PID算法"><a href="#2-1-2-增量式PID算法" class="headerlink" title="2.1.2 增量式PID算法"></a>2.1.2 增量式PID算法</h4><p><img src="/2024/01/30/STM32%E5%AE%9A%E6%97%B6%E5%99%A8%E4%B8%8EPID%E7%AE%97%E6%B3%95/1707463993099.png" alt="增量式PID"></p>
<p>比例P :    e(k)-e(k-1)   这次误差-上次误差</p>
<p>积分I :   e(i)     误差   </p>
<p>微分D :  e(k) - 2e(k-1)+e(k-2)   这次误差-2*上次误差+上上次误差</p>
<p> 增量式PID根据公式可以很好地看出，一旦确定了 KP、TI  、TD，只要使用前后三次测量值的偏差， 即可由公式求出控制增量</p>
<p>而得出的控制量▲u(k)对应的是近几次位置误差的增量，而不是对应与实际位置的偏差     <strong>没有误差累加</strong></p>
<p>也就是说，增量式PID中不需要累加。控制增量Δu(k)的确定仅与最近3次的采样值有关，容易通过加权处理获得比较好的控制效果，并且在系统发生问题时，增量式不会严重影响系统的工作</p>
<h3 id="2-2-直流电机PID闭环"><a href="#2-2-直流电机PID闭环" class="headerlink" title="2.2 直流电机PID闭环"></a>2.2 直流电机PID闭环</h3><blockquote>
<p>测试使用的电机的减速比为30，自带霍尔编码器的精度为13，AB相组合得到4倍频 =&gt; 电机转一圈编码器读数为$$30<em>13</em>4=1560$$。若每10ms读一次编码器则 $$编码器的速度 = 编码器读数/0.01s/1560(r/s)$$</p>
<p>由于电机本身的误差，可以添加误差系数。</p>
</blockquote>
<h4 id="2-2-1-STM32代码环境介绍"><a href="#2-2-1-STM32代码环境介绍" class="headerlink" title="2.2.1 STM32代码环境介绍"></a>2.2.1 STM32代码环境介绍</h4><h5 id="需求和目标："><a href="#需求和目标：" class="headerlink" title="需求和目标："></a><strong>需求和目标：</strong></h5><ul>
<li>创建Timer.c文件，用TIM3形成10ms（即100HZ）的定时中断，用于后续每10ms读取电机编码器的计数值。</li>
<li>创建Encoder.c文件，分别用TIM2和TIM4控制左右两个电机的编码器读数。</li>
<li>创建TB6612.c文件，高级定时器TIM1用来输出电机控制的PWM信号，以及电机的正反转。</li>
<li>创建PID.c文件，编写PID控制算法函数。</li>
</ul>
<h5 id="Timer-c文件"><a href="#Timer-c文件" class="headerlink" title="Timer.c文件"></a><strong>Timer.c文件</strong></h5><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"stm32f10x.h"</span>                  <span class="hljs-comment">// Device header</span></span>

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：定时中断初始化</span>
<span class="hljs-comment">  * 参    数：无</span>
<span class="hljs-comment">  * 返 回 值：无</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">Timer_Init</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-comment">/*开启时钟*/</span>
 RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM3, ENABLE);			<span class="hljs-comment">//开启TIM2的时钟</span>
 
 <span class="hljs-comment">/*配置时钟源*/</span>
 TIM_InternalClockConfig(TIM3);		<span class="hljs-comment">//选择TIM2为内部时钟，若不调用此函数，TIM默认也为内部时钟</span>
 
 <span class="hljs-comment">/*时基单元初始化*/</span>
 TIM_TimeBaseInitTypeDef TIM_TimeBaseInitStructure;				<span class="hljs-comment">//定义结构体变量</span>
 TIM_TimeBaseInitStructure.TIM_ClockDivision = TIM_CKD_DIV1;		<span class="hljs-comment">//时钟分频，选择不分频，此参数用于配置滤波器时钟，不影响时基单元功能</span>
 TIM_TimeBaseInitStructure.TIM_CounterMode = TIM_CounterMode_Up;	<span class="hljs-comment">//计数器模式，选择向上计数</span>
 TIM_TimeBaseInitStructure.TIM_Period = <span class="hljs-number">10000</span> - <span class="hljs-number">1</span>;				<span class="hljs-comment">//计数周期，即ARR的值</span>
 TIM_TimeBaseInitStructure.TIM_Prescaler = <span class="hljs-number">72</span> - <span class="hljs-number">1</span>;				<span class="hljs-comment">//预分频器，即PSC的值</span>
 TIM_TimeBaseInitStructure.TIM_RepetitionCounter = <span class="hljs-number">0</span>;			<span class="hljs-comment">//重复计数器，高级定时器才会用到</span>
 TIM_TimeBaseInit(TIM3, &amp;TIM_TimeBaseInitStructure);				<span class="hljs-comment">//将结构体变量交给TIM_TimeBaseInit，配置TIM2的时基单元	</span>
 
 <span class="hljs-comment">/*中断输出配置*/</span>
 TIM_ClearFlag(TIM3, TIM_FLAG_Update);						<span class="hljs-comment">//清除定时器更新标志位</span>
                <span class="hljs-comment">//TIM_TimeBaseInit函数末尾，手动产生了更新事件</span>
                <span class="hljs-comment">//若不清除此标志位，则开启中断后，会立刻进入一次中断</span>
                <span class="hljs-comment">//如果不介意此问题，则不清除此标志位也可</span>
 
 TIM_ITConfig(TIM3, TIM_IT_Update, ENABLE);					<span class="hljs-comment">//开启TIM2的更新中断</span>
 
 <span class="hljs-comment">/*NVIC中断分组*/</span>
 NVIC_PriorityGroupConfig(NVIC_PriorityGroup_2);				<span class="hljs-comment">//配置NVIC为分组2</span>
                <span class="hljs-comment">//即抢占优先级范围：0~3，响应优先级范围：0~3</span>
                <span class="hljs-comment">//此分组配置在整个工程中仅需调用一次</span>
                <span class="hljs-comment">//若有多个中断，可以把此代码放在main函数内，while循环之前</span>
                <span class="hljs-comment">//若调用多次配置分组的代码，则后执行的配置会覆盖先执行的配置</span>
 
 <span class="hljs-comment">/*NVIC配置*/</span>
 NVIC_InitTypeDef NVIC_InitStructure;						<span class="hljs-comment">//定义结构体变量</span>
 NVIC_InitStructure.NVIC_IRQChannel = TIM3_IRQn;				<span class="hljs-comment">//选择配置NVIC的TIM2线</span>
 NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;				<span class="hljs-comment">//指定NVIC线路使能</span>
 NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority = <span class="hljs-number">2</span>;	<span class="hljs-comment">//指定NVIC线路的抢占优先级为2</span>
 NVIC_InitStructure.NVIC_IRQChannelSubPriority = <span class="hljs-number">1</span>;			<span class="hljs-comment">//指定NVIC线路的响应优先级为1</span>
 NVIC_Init(&amp;NVIC_InitStructure);								<span class="hljs-comment">//将结构体变量交给NVIC_Init，配置NVIC外设</span>
 
 <span class="hljs-comment">/*TIM使能*/</span>
 TIM_Cmd(TIM3, ENABLE);			<span class="hljs-comment">//使能TIM2，定时器开始运行</span>
}

<span class="hljs-comment">/* 定时器中断函数，可以复制到使用它的地方</span>
<span class="hljs-comment">void TIM3_IRQHandler(void)</span>
<span class="hljs-comment">{</span>
<span class="hljs-comment"> if (TIM_GetITStatus(TIM3, TIM_IT_Update) == SET)</span>
<span class="hljs-comment"> {</span>
<span class="hljs-comment">  </span>
<span class="hljs-comment">  TIM_ClearITPendingBit(TIM3, TIM_IT_Update);</span>
<span class="hljs-comment"> }</span>
<span class="hljs-comment">}</span>
<span class="hljs-comment">*/</span></code></pre>

<h5 id="TB6612-c文件"><a href="#TB6612-c文件" class="headerlink" title="TB6612.c文件"></a><strong>TB6612.c文件</strong></h5><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"stm32f10x.h"</span>                  <span class="hljs-comment">// Device header</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"TB6612.h"</span></span>

<span class="hljs-type">void</span> <span class="hljs-title function_">TB6612_Init</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA|RCC_APB2Periph_GPIOB, ENABLE);
 
 <span class="hljs-comment">//电机PWM引脚(PA8,PB11)</span>
 GPIO_InitTypeDef GPIO_InitStructure;
 GPIO_InitStructure.GPIO_Mode = GPIO_Mode_AF_PP;
 GPIO_InitStructure.GPIO_Pin = GPIO_Pin_8|GPIO_Pin_11;
 GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
 
 GPIO_Init(GPIOA, &amp;GPIO_InitStructure);
 <span class="hljs-comment">//电机正负极引脚(PB12,PB13,PB14,PB15 )</span>
 GPIO_InitStructure.GPIO_Mode = GPIO_Mode_Out_PP;
 GPIO_InitStructure.GPIO_Pin = GPIO_Pin_12|GPIO_Pin_13|GPIO_Pin_14|GPIO_Pin_15;
 GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
 
 GPIO_Init(GPIOB, &amp;GPIO_InitStructure);
}

<span class="hljs-type">void</span> <span class="hljs-title function_">TB6612_PWM_Init</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 RCC_APB2PeriphClockCmd(RCC_APB2Periph_TIM1, ENABLE);
 
 
 TIM_TimeBaseInitTypeDef TIM_TimeBaseInitStructure;
 
 TIM_TimeBaseInitStructure.TIM_Period= <span class="hljs-number">100</span>;
 TIM_TimeBaseInitStructure.TIM_Prescaler = <span class="hljs-number">7200</span><span class="hljs-number">-1</span>;
 TIM_TimeBaseInitStructure.TIM_CounterMode = TIM_CounterMode_Up;
 TIM_TimeBaseInitStructure.TIM_ClockDivision = TIM_CKD_DIV1;
 TIM_TimeBaseInitStructure.TIM_RepetitionCounter = <span class="hljs-number">0</span>;
 TIM_InternalClockConfig(TIM1);	
 TIM_TimeBaseInit(TIM1, &amp;TIM_TimeBaseInitStructure);
 
 TIM_OCInitTypeDef TIM_OCInitStructure;
 
 TIM_OCStructInit(&amp;TIM_OCInitStructure);
 TIM_OCInitStructure.TIM_OCMode = TIM_OCMode_PWM1;
 TIM_OCInitStructure.TIM_Pulse = <span class="hljs-number">0</span>;
 TIM_OCInitStructure.TIM_OCPolarity = TIM_OCPolarity_High;
 TIM_OCInitStructure.TIM_OutputState = TIM_OutputState_Enable;
 
 TIM_OC1Init(TIM1, &amp;TIM_OCInitStructure);
 TIM_OC4Init(TIM1, &amp;TIM_OCInitStructure);

 TIM_Cmd(TIM1, ENABLE);
 <span class="hljs-comment">//设置TIMx的PWM主输出为使能，高级定时器不使用从模式的话都要加这一句；</span>
 TIM_CtrlPWMOutputs(TIM1,ENABLE); 
}

<span class="hljs-type">void</span> <span class="hljs-title function_">Motor_SetSpeed</span><span class="hljs-params">(<span class="hljs-type">int8_t</span> SpeedL, <span class="hljs-type">int8_t</span> SpeedR)</span>
{
 <span class="hljs-keyword">if</span>(SpeedL&gt;=<span class="hljs-number">0</span>)
 {
  TIM_SetCompare1(TIM1, SpeedL);
  Motor_StartL(<span class="hljs-number">1</span>);
 }
 <span class="hljs-keyword">else</span>
 {
  TIM_SetCompare1(TIM1, -SpeedL);
  Motor_StartL(<span class="hljs-number">0</span>);
 }
 <span class="hljs-keyword">if</span>(SpeedR&gt;=<span class="hljs-number">0</span>)
 {
  TIM_SetCompare4(TIM1, SpeedR);
  Motor_StartR(<span class="hljs-number">1</span>);
 }
 <span class="hljs-keyword">else</span>
 {
  TIM_SetCompare4(TIM1, -SpeedR);
  Motor_StartR(<span class="hljs-number">0</span>);
 }
}

<span class="hljs-type">void</span> <span class="hljs-title function_">MotorEnable</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> State)</span>
{
 GPIO_WriteBit(GPIOA, GPIO_Pin_11, (BitAction)State);
 GPIO_WriteBit(GPIOA, GPIO_Pin_8, (BitAction)State);
}

<span class="hljs-type">void</span> <span class="hljs-title function_">Motor_StartL</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> A01)</span>
{
 <span class="hljs-type">uint8_t</span> A02;
 GPIO_WriteBit(GPIOB, GPIO_Pin_14, (BitAction)A01);
 A02=A01^<span class="hljs-number">0x01</span>;
 GPIO_WriteBit(GPIOB, GPIO_Pin_15, (BitAction)A02);
}

<span class="hljs-type">void</span> <span class="hljs-title function_">Motor_StartR</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> B01)</span>
{
 <span class="hljs-type">uint8_t</span> B02;
 GPIO_WriteBit(GPIOB, GPIO_Pin_13, (BitAction)B01);
 B02=B01^<span class="hljs-number">0x01</span>;
 GPIO_WriteBit(GPIOB, GPIO_Pin_12, (BitAction)B02);
}
</code></pre>

<h5 id="Encoder-c文件"><a href="#Encoder-c文件" class="headerlink" title="Encoder.c文件"></a>Encoder.c文件</h5><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"stm32f10x.h"</span>                  <span class="hljs-comment">// Device header</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"PID.h"</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"Encoder.h"</span></span>

<span class="hljs-type">void</span> <span class="hljs-title function_">Encoder_Init</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-comment">/*开启时钟*/</span>
 RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM2, ENABLE);			<span class="hljs-comment">//开启TIM3的时钟</span>
 RCC_APB1PeriphClockCmd(RCC_APB1Periph_TIM4, ENABLE);
 RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA, ENABLE);			<span class="hljs-comment">//开启GPIOA的时钟</span>
 RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOB, ENABLE);
 <span class="hljs-comment">/*GPIO初始化*/</span>
 GPIO_InitTypeDef GPIO_InitStructure;
 GPIO_InitStructure.GPIO_Mode = GPIO_Mode_IPU;
 GPIO_InitStructure.GPIO_Pin = GPIO_Pin_0 | GPIO_Pin_1;
 GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
 GPIO_Init(GPIOA, &amp;GPIO_InitStructure);							<span class="hljs-comment">//将PA6和PA7引脚初始化为上拉输入</span>

 GPIO_InitStructure.GPIO_Mode = GPIO_Mode_IPU;
 GPIO_InitStructure.GPIO_Pin = GPIO_Pin_6 | GPIO_Pin_7;
 GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
 GPIO_Init(GPIOB, &amp;GPIO_InitStructure);	
 <span class="hljs-comment">/*时基单元初始化*/</span>
 TIM_TimeBaseInitTypeDef TIM_TimeBaseInitStructure;				<span class="hljs-comment">//定义结构体变量</span>
 TIM_TimeBaseInitStructure.TIM_ClockDivision = TIM_CKD_DIV1;     <span class="hljs-comment">//时钟分频，选择不分频，此参数用于配置滤波器时钟，不影响时基单元功能</span>
 TIM_TimeBaseInitStructure.TIM_CounterMode = TIM_CounterMode_Up; <span class="hljs-comment">//计数器模式，选择向上计数</span>
 TIM_TimeBaseInitStructure.TIM_Period = <span class="hljs-number">65536</span> - <span class="hljs-number">1</span>;               <span class="hljs-comment">//计数周期，即ARR的值</span>
 TIM_TimeBaseInitStructure.TIM_Prescaler = <span class="hljs-number">1</span> - <span class="hljs-number">1</span>;                <span class="hljs-comment">//预分频器，即PSC的值</span>
 TIM_TimeBaseInitStructure.TIM_RepetitionCounter = <span class="hljs-number">0</span>;            <span class="hljs-comment">//重复计数器，高级定时器才会用到</span>
 TIM_TimeBaseInit(TIM2, &amp;TIM_TimeBaseInitStructure);             <span class="hljs-comment">//将结构体变量交给TIM_TimeBaseInit，配置TIM3的时基单元</span>
 
 TIM_TimeBaseInitStructure.TIM_ClockDivision = TIM_CKD_DIV1;     <span class="hljs-comment">//时钟分频，选择不分频，此参数用于配置滤波器时钟，不影响时基单元功能</span>
 TIM_TimeBaseInitStructure.TIM_CounterMode = TIM_CounterMode_Up; <span class="hljs-comment">//计数器模式，选择向上计数</span>
 TIM_TimeBaseInitStructure.TIM_Period = <span class="hljs-number">65536</span> - <span class="hljs-number">1</span>;               <span class="hljs-comment">//计数周期，即ARR的值</span>
 TIM_TimeBaseInitStructure.TIM_Prescaler = <span class="hljs-number">1</span> - <span class="hljs-number">1</span>;                <span class="hljs-comment">//预分频器，即PSC的值</span>
 TIM_TimeBaseInitStructure.TIM_RepetitionCounter = <span class="hljs-number">0</span>;            <span class="hljs-comment">//重复计数器，高级定时器才会用到</span>
 TIM_TimeBaseInit(TIM4, &amp;TIM_TimeBaseInitStructure);             <span class="hljs-comment">//将结构体变量交给TIM_TimeBaseInit，配置TIM3的时基单元</span>
 
 <span class="hljs-comment">/*输入捕获初始化*/</span>
 TIM_ICInitTypeDef TIM_ICInitStructure;							<span class="hljs-comment">//定义结构体变量</span>
 TIM_ICStructInit(&amp;TIM_ICInitStructure);							<span class="hljs-comment">//结构体初始化，若结构体没有完整赋值</span>
                 <span class="hljs-comment">//则最好执行此函数，给结构体所有成员都赋一个默认值</span>
                 <span class="hljs-comment">//避免结构体初值不确定的问题</span>
 TIM_ICInitStructure.TIM_Channel = TIM_Channel_1;				<span class="hljs-comment">//选择配置定时器通道1</span>
 TIM_ICInitStructure.TIM_ICFilter = <span class="hljs-number">0xF</span>;							<span class="hljs-comment">//输入滤波器参数，可以过滤信号抖动</span>
 TIM_ICInit(TIM2, &amp;TIM_ICInitStructure);							<span class="hljs-comment">//将结构体变量交给TIM_ICInit，配置TIM3的输入捕获通道</span>
 TIM_ICInitStructure.TIM_Channel = TIM_Channel_2;				<span class="hljs-comment">//选择配置定时器通道2</span>
 TIM_ICInitStructure.TIM_ICFilter = <span class="hljs-number">0xF</span>;							<span class="hljs-comment">//输入滤波器参数，可以过滤信号抖动</span>
 TIM_ICInit(TIM2, &amp;TIM_ICInitStructure);							<span class="hljs-comment">//将结构体变量交给TIM_ICInit，配置TIM3的输入捕获通道</span>
 
 TIM_ICStructInit(&amp;TIM_ICInitStructure);							<span class="hljs-comment">//结构体初始化，若结构体没有完整赋值</span>
                 <span class="hljs-comment">//则最好执行此函数，给结构体所有成员都赋一个默认值</span>
                 <span class="hljs-comment">//避免结构体初值不确定的问题</span>
 TIM_ICInitStructure.TIM_Channel = TIM_Channel_1;				<span class="hljs-comment">//选择配置定时器通道1</span>
 TIM_ICInitStructure.TIM_ICFilter = <span class="hljs-number">0xF</span>;							<span class="hljs-comment">//输入滤波器参数，可以过滤信号抖动</span>
 TIM_ICInit(TIM4, &amp;TIM_ICInitStructure);							<span class="hljs-comment">//将结构体变量交给TIM_ICInit，配置TIM3的输入捕获通道</span>
 TIM_ICInitStructure.TIM_Channel = TIM_Channel_2;				<span class="hljs-comment">//选择配置定时器通道2</span>
 TIM_ICInitStructure.TIM_ICFilter = <span class="hljs-number">0xF</span>;							<span class="hljs-comment">//输入滤波器参数，可以过滤信号抖动</span>
 TIM_ICInit(TIM4, &amp;TIM_ICInitStructure);		
 <span class="hljs-comment">/*编码器接口配置*/</span>
 TIM_EncoderInterfaceConfig(TIM2, TIM_EncoderMode_TI12, TIM_ICPolarity_Rising, TIM_ICPolarity_Rising);
 TIM_Cmd(TIM2, ENABLE);			<span class="hljs-comment">//使能TIM2，定时器开始运行</span>
 TIM_EncoderInterfaceConfig(TIM4, TIM_EncoderMode_TI12, TIM_ICPolarity_Falling, TIM_ICPolarity_Rising);
 <span class="hljs-comment">/*TIM使能*/</span>
 
 TIM_Cmd(TIM4, ENABLE);
}


<span class="hljs-type">int16_t</span> <span class="hljs-title function_">Encoder_Get</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-type">int16_t</span> Temp;
 Temp = TIM_GetCounter(TIM2);
 TIM_SetCounter(TIM2, <span class="hljs-number">0</span>);
 <span class="hljs-keyword">return</span> Temp;
}

<span class="hljs-type">int16_t</span> <span class="hljs-title function_">Encoder_Get_</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-type">int16_t</span> Temp1;
 Temp1 = TIM_GetCounter(TIM4);
 TIM_SetCounter(TIM4, <span class="hljs-number">0</span>);
 <span class="hljs-keyword">return</span> Temp1;
}

<span class="hljs-type">int16_t</span> Speed1,Speed2;
<span class="hljs-type">int</span> Position1,Position2;
<span class="hljs-type">int16_t</span> PWM_P1, PWM_V1, PWM_P2, PWM_V2;
<span class="hljs-type">int16_t</span> PWM_OUT1,PWM_OUT2;

<span class="hljs-type">void</span> <span class="hljs-title function_">TIM3_IRQHandler</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-keyword">if</span> (TIM_GetITStatus(TIM3, TIM_IT_Update) == SET)
 {
  Speed1 = Encoder_Get();
  Speed2 = Encoder_Get_();
  Position1 += Speed1;
  Position2 += Speed2;
<span class="hljs-comment">//  //位置闭环</span>
<span class="hljs-comment">//  PWM_P1 = Position_FeedbackControl(&amp;PID_MotorPosition, Position1);</span>
<span class="hljs-comment">//  PWM_P2 = Position_FeedbackControl(&amp;PID_MotorPosition, Position2);</span>
<span class="hljs-comment">//  //限幅PWM</span>
<span class="hljs-comment">//  PWM_P1 = Velocity_Restrict(PWM_P1, &amp;PID_MotorVelocity);</span>
<span class="hljs-comment">//  PWM_P2 = Velocity_Restrict(PWM_P2, &amp;PID_MotorVelocity);</span>
<span class="hljs-comment">//  /*</span>
<span class="hljs-comment">//  位置环输出的 PWM 值按一定比例转换为速度值，接下来使用该速度值进行速度闭环控制。</span>
<span class="hljs-comment">//  空载时， 电源适配器供电情况下，每76PWM 约等于 1 编码器速度</span>
<span class="hljs-comment">//  */</span>
<span class="hljs-comment">//  PWM_V1 = PWM_P1/76;</span>
<span class="hljs-comment">//  PWM_V2 = PWM_P2/76;</span>
<span class="hljs-comment">//  //速度闭环</span>
<span class="hljs-comment">//  PID_MotorVelocity.target_val=PWM_V1;</span>
<span class="hljs-comment">//  PWM_OUT1 = Velocity_FeedbackControl(&amp;PID_MotorVelocity, Speed1);</span>
<span class="hljs-comment">//  PID_MotorVelocity.target_val=PWM_V2;</span>
<span class="hljs-comment">//  PWM_OUT2 = Velocity_FeedbackControl(&amp;PID_MotorVelocity, Speed2);</span>
  
  TIM_ClearITPendingBit(TIM3, TIM_IT_Update);
 }
}
</code></pre>

<h5 id="PID-c文件"><a href="#PID-c文件" class="headerlink" title="PID.c文件"></a>PID.c文件</h5><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"stm32f10x.h"</span>      </span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"PID.h"</span>                  </span>

tPid PID_MotorVelocity;
tPid PID_MotorPosition;
<span class="hljs-type">void</span> <span class="hljs-title function_">PID_MotorVelocity_Init</span><span class="hljs-params">(<span class="hljs-type">float</span> speed)</span>
{
 <span class="hljs-comment">//0~80稳定</span>
 PID_MotorVelocity.target_val = speed;
 PID_MotorVelocity.actual_val = <span class="hljs-number">0</span>;
 PID_MotorVelocity.err = <span class="hljs-number">0</span>;
 PID_MotorVelocity.last_err = <span class="hljs-number">0</span>;
 PID_MotorVelocity.sum_err = <span class="hljs-number">0</span>;
 PID_MotorVelocity.Kp = <span class="hljs-number">8.2</span>;
 PID_MotorVelocity.Ki = <span class="hljs-number">0.15</span>;
 PID_MotorVelocity.Kd = <span class="hljs-number">1.2</span>;
 PID_MotorVelocity.control_val =<span class="hljs-number">0</span> ;
}
<span class="hljs-type">void</span> <span class="hljs-title function_">PID_MotorPosition_Init</span><span class="hljs-params">(<span class="hljs-type">float</span> circle)</span>
{
 <span class="hljs-comment">//0~80稳定</span>
 PID_MotorPosition.target_val = circle*<span class="hljs-number">1560</span>;
 PID_MotorPosition.actual_val = <span class="hljs-number">0</span>;
 PID_MotorPosition.err = <span class="hljs-number">0</span>;
 PID_MotorPosition.last_err = <span class="hljs-number">0</span>;
 PID_MotorPosition.sum_err = <span class="hljs-number">0</span>;
 PID_MotorPosition.Kp = <span class="hljs-number">3.8</span>;
 PID_MotorPosition.Ki = <span class="hljs-number">0</span>;
 PID_MotorPosition.Kd = <span class="hljs-number">0</span>;
 PID_MotorPosition.control_val =<span class="hljs-number">0</span> ;
}
<span class="hljs-comment">//速度闭环</span>
<span class="hljs-type">int16_t</span> <span class="hljs-title function_">Velocity_FeedbackControl</span><span class="hljs-params">(tPid *pid, <span class="hljs-type">int16_t</span> actual_val)</span>
{
 pid-&gt;actual_val = actual_val; 
 pid-&gt;err = pid-&gt;target_val - pid-&gt;actual_val; 
 pid-&gt;sum_err+=pid-&gt;err; 
 pid-&gt;control_val = pid-&gt;Kp*pid-&gt;err + pid-&gt;Ki*pid-&gt;sum_err + pid-&gt;Kd*(pid-&gt;err-pid-&gt;last_err);
 pid-&gt;last_err = pid-&gt;err; 
 <span class="hljs-keyword">return</span> pid-&gt;control_val;
}
<span class="hljs-comment">//位置闭环</span>
<span class="hljs-type">int16_t</span> <span class="hljs-title function_">Position_FeedbackControl</span><span class="hljs-params">(tPid *pid, <span class="hljs-type">int16_t</span> actual_val)</span>
{
 pid-&gt;actual_val = actual_val;
 pid-&gt;err = pid-&gt;target_val - pid-&gt;actual_val;
 pid-&gt;sum_err+=pid-&gt;err; 
 pid-&gt;control_val = pid-&gt;Kp*pid-&gt;err + pid-&gt;Ki*pid-&gt;sum_err + pid-&gt;Kd*(pid-&gt;err-pid-&gt;last_err);
 pid-&gt;last_err = pid-&gt;err; 
 <span class="hljs-keyword">return</span> pid-&gt;control_val;
}
<span class="hljs-type">int16_t</span> <span class="hljs-title function_">Velocity_Restrict</span><span class="hljs-params">(<span class="hljs-type">int16_t</span> PWM_P, tPid *pid)</span>
{
 <span class="hljs-keyword">if</span> (PWM_P &gt; +pid-&gt;target_val*<span class="hljs-number">76</span>) PWM_P = +pid-&gt;target_val*<span class="hljs-number">76</span>;
 <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (PWM_P &lt; -pid-&gt;target_val*<span class="hljs-number">76</span>) PWM_P = -pid-&gt;target_val*<span class="hljs-number">76</span>;
 <span class="hljs-keyword">else</span> PWM_P=PWM_P;
 <span class="hljs-keyword">return</span> PWM_P;
}
</code></pre>

<p>PID.h中定义该结构体</p>
<pre><code class="hljs c"><span class="hljs-keyword">typedef</span> <span class="hljs-class"><span class="hljs-keyword">struct</span></span>
<span class="hljs-class">{</span>
 <span class="hljs-type">float</span> target_val;<span class="hljs-comment">//目标值</span>
 <span class="hljs-type">int16_t</span> actual_val;<span class="hljs-comment">//真实值</span>
 <span class="hljs-type">int16_t</span> err;       <span class="hljs-comment">//偏差值</span>
 <span class="hljs-type">int16_t</span> last_err;  <span class="hljs-comment">//上一次的偏差</span>
 <span class="hljs-type">int16_t</span> sum_err;   <span class="hljs-comment">//累计偏差</span>
 <span class="hljs-type">float</span> Kp, Ki, Kd;  <span class="hljs-comment">//pid参数</span>
 <span class="hljs-type">int16_t</span> control_val; <span class="hljs-comment">//控制值</span>

}tPid;
<span class="hljs-comment">// 声明PID结构体</span>
<span class="hljs-keyword">extern</span> tPid PID_MotorVelocity;
<span class="hljs-keyword">extern</span> tPid PID_MotorPosition;</code></pre>

]]></content>
      <categories>
        <category>STM32</category>
      </categories>
      <tags>
        <tag>STM32</tag>
        <tag>PID</tag>
      </tags>
  </entry>
  <entry>
    <title>STM32四大通信协议</title>
    <url>/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/</url>
    <content><![CDATA[<h2 id="1-USART串口通信"><a href="#1-USART串口通信" class="headerlink" title="1. USART串口通信"></a>1. USART串口通信</h2><blockquote>
<p>STM32–串口通信</p>
</blockquote>
<h3 id="1-1-串口通信协议"><a href="#1-1-串口通信协议" class="headerlink" title="1.1 串口通信协议"></a>1.1 串口通信协议</h3><ul>
<li><strong>通信的目的：</strong>将一个设备的数据传送到另一个设备，扩展硬件系统</li>
<li><strong>通信协议：</strong>制定通信的规则，通信双方按照协议规则进行数据收发</li>
</ul>
<table>
<thead>
<tr>
<th><strong>名称</strong></th>
<th><strong>引脚</strong></th>
<th><strong>双工</strong></th>
<th><strong>时钟</strong></th>
<th><strong>电平</strong></th>
<th><strong>设备</strong></th>
</tr>
</thead>
<tbody><tr>
<td>USART</td>
<td>TX、RX</td>
<td>全双工</td>
<td>异步</td>
<td>单端</td>
<td>点对点</td>
</tr>
<tr>
<td>I2C</td>
<td>SCL、SDA</td>
<td>半双工</td>
<td>同步</td>
<td>单端</td>
<td>多设备</td>
</tr>
<tr>
<td>SPI</td>
<td>SCLK、MOSI、MISO、CS</td>
<td>全双工</td>
<td>同步</td>
<td>单端</td>
<td>多设备</td>
</tr>
<tr>
<td>CAN</td>
<td>CAN_H、CAN_L</td>
<td>半双工</td>
<td>异步</td>
<td>差分</td>
<td>多设备</td>
</tr>
<tr>
<td>USB</td>
<td>DP、DM</td>
<td>半双工</td>
<td>异步</td>
<td>差分</td>
<td>点对点</td>
</tr>
</tbody></table>
<blockquote>
<p>双工：指通信双方是否可以同时进行双向通信（发送和接收）。</p>
<p>单工：数据只能从一个设备到另一个设备，不能反着来。</p>
<p>时钟： I2C 和 SPI 有单独的时钟线，所以是同步。</p>
<p>电平：单端信号，即引脚的高低电平是相对GND的电压差，所以需要共地。</p>
</blockquote>
<span id="more"></span>

<h3 id="1-2-USART简介"><a href="#1-2-USART简介" class="headerlink" title="1.2 USART简介"></a>1.2 USART简介</h3><ul>
<li>串口是一种应用十分广泛的通讯接口，串口成本低、容易使用、通信线路简单，可实现两个设备的互相通信</li>
<li>单片机的串口可以使单片机与单片机、单片机与电脑、单片机与各式各样的模块互相通信，极大地扩展了单片机的应用范围，增强了单片机系统的硬件实力</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706363962945.png" alt="蓝牙模块"></p>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/USB%E8%BD%AC%E4%B8%B2%E5%8F%A3.png" alt="USB转串口"></p>
<h4 id="1-2-1硬件电路"><a href="#1-2-1硬件电路" class="headerlink" title="1.2.1硬件电路"></a>1.2.1硬件电路</h4><ul>
<li>简单双向串口通信有两根通信线（发送端TX和接收端RX）</li>
<li>TX与RX要交叉连接</li>
<li>当只需单向的数据传输时，可以只接一根通信线</li>
<li>当电平标准不一致时，需要加电平转换芯片</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706364052710.png" alt="串口电路"></p>
<h4 id="1-2-2-电平标准"><a href="#1-2-2-电平标准" class="headerlink" title="1.2.2 电平标准"></a>1.2.2 电平标准</h4><ul>
<li>电平标准是数据1和数据0的表达方式，是传输线缆中人为规定的电压与数据的对应关系，串口常用的电平标准有如下三种：</li>
<li>TTL电平：+3.3V或+5V表示1，0V表示0</li>
<li>RS232电平：-3 至 -15V表示1，+3 至 +15V表示0</li>
<li>RS485电平：两线压差+2至+6V表示1，-2 至 -6V表示0（差分信号）</li>
</ul>
<h4 id="1-2-3-串口参数及时序"><a href="#1-2-3-串口参数及时序" class="headerlink" title="1.2.3 串口参数及时序"></a>1.2.3 串口参数及时序</h4><ul>
<li><strong>波特率：</strong>串口通信的速率</li>
<li><strong>起始位：</strong>标志一个数据帧的开始，<strong>固定为低电平</strong></li>
<li><strong>数据位：</strong>数据帧的有效载荷，1为高电平，0为低电平，低位先行</li>
<li><strong>校验位：</strong>用于数据验证，根据数据位计算得来(无校验、奇校验–即保持二进制数据1的个数为奇数、偶校验)</li>
<li><strong>停止位：</strong>用于数据帧间隔，<strong>固定为高电平</strong></li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706365185213.png" alt="串口时序"></p>
<h4 id="1-2-4-串口时序"><a href="#1-2-4-串口时序" class="headerlink" title="1.2.4 串口时序"></a>1.2.4 串口时序</h4><p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706364301642.png" alt="串口时序波形"></p>
<h3 id="1-3-USART简介"><a href="#1-3-USART简介" class="headerlink" title="1.3 USART简介"></a>1.3 USART简介</h3><ul>
<li>USART（Universal Synchronous/Asynchronous Receiver/Transmitter）通用同步/异步收发器</li>
<li>USART是STM32内部集成的硬件外设，可根据数据寄存器的一个字节数据自动生成数据帧时序，从TX引脚发送出去，也可自动接收RX引脚的数据帧时序，拼接为一个字节数据，存放在数据寄存器里</li>
<li>自带波特率发生器，最高达4.5Mbits/s</li>
<li>可配置数据位长度（8/9）、停止位长度（0.5/1/1.5/2）</li>
<li>可选校验位（无校验/奇校验/偶校验）</li>
<li>支持同步模式、硬件流控制、DMA、智能卡、IrDA、LIN</li>
<li>STM32F103C8T6 USART资源： USART1、 USART2、 USART3</li>
</ul>
<h4 id="1-3-1-USART框图"><a href="#1-3-1-USART框图" class="headerlink" title="1.3.1 USART框图"></a>1.3.1 USART框图</h4><p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706364443610.png" alt="USART狂徒"></p>
<h4 id="1-3-2-USART基本结构"><a href="#1-3-2-USART基本结构" class="headerlink" title="1.3.2 USART基本结构"></a>1.3.2 USART基本结构</h4><p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706365964060.png" alt="USART基本结构"></p>
<h4 id="1-3-3-数据帧"><a href="#1-3-3-数据帧" class="headerlink" title="1.3.3 数据帧"></a>1.3.3 数据帧</h4><p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706366018154.png" alt="字长设置"></p>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706366029738.png" alt="配置停止位"></p>
<h4 id="1-3-4-起始位侦测"><a href="#1-3-4-起始位侦测" class="headerlink" title="1.3.4 起始位侦测"></a>1.3.4 起始位侦测</h4><p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706366050574.png" alt="起始位侦测"></p>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706366062786.png" alt="噪声检测"></p>
<h4 id="1-3-6-波特率发生器"><a href="#1-3-6-波特率发生器" class="headerlink" title="1.3.6 波特率发生器"></a>1.3.6 波特率发生器</h4><ul>
<li>发送器和接收器的波特率由波特率寄存器BRR里的DIV确定</li>
<li>计算公式：波特率 = f_PCLK2/1 / (16 * DIV)​</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706366102368.png" alt="波特率寄存器"></p>
<h3 id="1-4-USART代码示例"><a href="#1-4-USART代码示例" class="headerlink" title="1.4 USART代码示例"></a>1.4 USART代码示例</h3><pre><code class="hljs C"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"stm32f10x.h"</span>                  <span class="hljs-comment">// Device header</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;stdio.h&gt;</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;stdarg.h&gt;</span></span>

<span class="hljs-type">uint8_t</span> Serial_RxData;  <span class="hljs-comment">//定义串口接收的数据变量</span>
<span class="hljs-type">uint8_t</span> Serial_RxFlag;  <span class="hljs-comment">//定义串口接收的标志位变量</span>

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：串口初始化</span>
<span class="hljs-comment">  * 参    数：无</span>
<span class="hljs-comment">  * 返 回 值：无</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">Serial_Init</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-comment">/*开启时钟*/</span>
 RCC_APB2PeriphClockCmd(RCC_APB2Periph_USART1, ENABLE);	<span class="hljs-comment">//开启USART1的时钟</span>
 RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA, ENABLE);	<span class="hljs-comment">//开启GPIOA的时钟</span>
 
 <span class="hljs-comment">/*GPIO初始化*/</span>
 GPIO_InitTypeDef GPIO_InitStructure;
 GPIO_InitStructure.GPIO_Mode = GPIO_Mode_AF_PP;
 GPIO_InitStructure.GPIO_Pin = GPIO_Pin_9;
 GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
 GPIO_Init(GPIOA, &amp;GPIO_InitStructure);					<span class="hljs-comment">//将PA9引脚初始化为复用推挽输出</span>
 
 GPIO_InitStructure.GPIO_Mode = GPIO_Mode_IPU;
 GPIO_InitStructure.GPIO_Pin = GPIO_Pin_10;
 GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
 GPIO_Init(GPIOA, &amp;GPIO_InitStructure);					<span class="hljs-comment">//将PA10引脚初始化为上拉输入</span>
 
 <span class="hljs-comment">/*USART初始化*/</span>
 USART_InitTypeDef USART_InitStructure;					<span class="hljs-comment">//定义结构体变量</span>
 USART_InitStructure.USART_BaudRate = <span class="hljs-number">9600</span>;				<span class="hljs-comment">//波特率</span>
 USART_InitStructure.USART_HardwareFlowControl = USART_HardwareFlowControl_None;	<span class="hljs-comment">//硬件流控制，不需要</span>
 USART_InitStructure.USART_Mode = USART_Mode_Tx | USART_Mode_Rx;	<span class="hljs-comment">//模式，发送模式和接收模式均选择</span>
 USART_InitStructure.USART_Parity = USART_Parity_No;		<span class="hljs-comment">//奇偶校验，不需要</span>
 USART_InitStructure.USART_StopBits = USART_StopBits_1;	<span class="hljs-comment">//停止位，选择1位</span>
 USART_InitStructure.USART_WordLength = USART_WordLength_8b;		<span class="hljs-comment">//字长，选择8位</span>
 USART_Init(USART1, &amp;USART_InitStructure);				<span class="hljs-comment">//将结构体变量交给USART_Init，配置USART1</span>
 
 <span class="hljs-comment">/*中断输出配置*/</span>
 USART_ITConfig(USART1, USART_IT_RXNE, ENABLE);			<span class="hljs-comment">//开启串口接收数据的中断</span>
 
 <span class="hljs-comment">/*NVIC中断分组*/</span>
 NVIC_PriorityGroupConfig(NVIC_PriorityGroup_2);			<span class="hljs-comment">//配置NVIC为分组2</span>
 
 <span class="hljs-comment">/*NVIC配置*/</span>
 NVIC_InitTypeDef NVIC_InitStructure;					<span class="hljs-comment">//定义结构体变量</span>
 NVIC_InitStructure.NVIC_IRQChannel = USART1_IRQn;		<span class="hljs-comment">//选择配置NVIC的USART1线</span>
 NVIC_InitStructure.NVIC_IRQChannelCmd = ENABLE;			<span class="hljs-comment">//指定NVIC线路使能</span>
 NVIC_InitStructure.NVIC_IRQChannelPreemptionPriority = <span class="hljs-number">1</span>;		<span class="hljs-comment">//指定NVIC线路的抢占优先级为1</span>
 NVIC_InitStructure.NVIC_IRQChannelSubPriority = <span class="hljs-number">1</span>;		<span class="hljs-comment">//指定NVIC线路的响应优先级为1</span>
 NVIC_Init(&amp;NVIC_InitStructure);							<span class="hljs-comment">//将结构体变量交给NVIC_Init，配置NVIC外设</span>
 
 <span class="hljs-comment">/*USART使能*/</span>
 USART_Cmd(USART1, ENABLE);								<span class="hljs-comment">//使能USART1，串口开始运行</span>
}

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：串口发送一个字节</span>
<span class="hljs-comment">  * 参    数：Byte 要发送的一个字节</span>
<span class="hljs-comment">  * 返 回 值：无</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">Serial_SendByte</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> Byte)</span>
{
 USART_SendData(USART1, Byte);		<span class="hljs-comment">//将字节数据写入数据寄存器，写入后USART自动生成时序波形</span>
 <span class="hljs-keyword">while</span> (USART_GetFlagStatus(USART1, USART_FLAG_TXE) == RESET);	<span class="hljs-comment">//等待发送完成</span>
 <span class="hljs-comment">/*下次写入数据寄存器会自动清除发送完成标志位，故此循环后，无需清除标志位*/</span>
}

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：串口发送一个数组</span>
<span class="hljs-comment">  * 参    数：Array 要发送数组的首地址</span>
<span class="hljs-comment">  * 参    数：Length 要发送数组的长度</span>
<span class="hljs-comment">  * 返 回 值：无</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">Serial_SendArray</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> *Array, <span class="hljs-type">uint16_t</span> Length)</span>
{
 <span class="hljs-type">uint16_t</span> i;
 <span class="hljs-keyword">for</span> (i = <span class="hljs-number">0</span>; i &lt; Length; i ++)		<span class="hljs-comment">//遍历数组</span>
 {
  Serial_SendByte(Array[i]);		<span class="hljs-comment">//依次调用Serial_SendByte发送每个字节数据</span>
 }
}

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：串口发送一个字符串</span>
<span class="hljs-comment">  * 参    数：String 要发送字符串的首地址</span>
<span class="hljs-comment">  * 返 回 值：无</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">Serial_SendString</span><span class="hljs-params">(<span class="hljs-type">char</span> *String)</span>
{
 <span class="hljs-type">uint8_t</span> i;
 <span class="hljs-keyword">for</span> (i = <span class="hljs-number">0</span>; String[i] != <span class="hljs-string">'\0'</span>; i ++)<span class="hljs-comment">//遍历字符数组（字符串），遇到字符串结束标志位后停止</span>
 {
  Serial_SendByte(String[i]);		<span class="hljs-comment">//依次调用Serial_SendByte发送每个字节数据</span>
 }
}

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：次方函数（内部使用）</span>
<span class="hljs-comment">  * 返 回 值：返回值等于X的Y次方</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">uint32_t</span> <span class="hljs-title function_">Serial_Pow</span><span class="hljs-params">(<span class="hljs-type">uint32_t</span> X, <span class="hljs-type">uint32_t</span> Y)</span>
{
 <span class="hljs-type">uint32_t</span> Result = <span class="hljs-number">1</span>;	<span class="hljs-comment">//设置结果初值为1</span>
 <span class="hljs-keyword">while</span> (Y --)			<span class="hljs-comment">//执行Y次</span>
 {
  Result *= X;		<span class="hljs-comment">//将X累乘到结果</span>
 }
 <span class="hljs-keyword">return</span> Result;
}

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：串口发送数字</span>
<span class="hljs-comment">  * 参    数：Number 要发送的数字，范围：0~4294967295</span>
<span class="hljs-comment">  * 参    数：Length 要发送数字的长度，范围：0~10</span>
<span class="hljs-comment">  * 返 回 值：无</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">Serial_SendNumber</span><span class="hljs-params">(<span class="hljs-type">uint32_t</span> Number, <span class="hljs-type">uint8_t</span> Length)</span>
{
 <span class="hljs-type">uint8_t</span> i;
 <span class="hljs-keyword">for</span> (i = <span class="hljs-number">0</span>; i &lt; Length; i ++)		<span class="hljs-comment">//根据数字长度遍历数字的每一位</span>
 {
  Serial_SendByte(Number / Serial_Pow(<span class="hljs-number">10</span>, Length - i - <span class="hljs-number">1</span>) % <span class="hljs-number">10</span> + <span class="hljs-string">'0'</span>);	<span class="hljs-comment">//依次调用Serial_SendByte发送每位数字</span>
 }
}

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：使用printf需要重定向的底层函数</span>
<span class="hljs-comment">  * 参    数：保持原始格式即可，无需变动</span>
<span class="hljs-comment">  * 返 回 值：保持原始格式即可，无需变动</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">int</span> <span class="hljs-title function_">fputc</span><span class="hljs-params">(<span class="hljs-type">int</span> ch, FILE *f)</span>
{
 Serial_SendByte(ch);			<span class="hljs-comment">//将printf的底层重定向到自己的发送字节函数</span>
 <span class="hljs-keyword">return</span> ch;
}

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：自己封装的prinf函数</span>
<span class="hljs-comment">  * 参    数：format 格式化字符串</span>
<span class="hljs-comment">  * 参    数：... 可变的参数列表</span>
<span class="hljs-comment">  * 返 回 值：无</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">Serial_Printf</span><span class="hljs-params">(<span class="hljs-type">char</span> *format, ...)</span>
{
 <span class="hljs-type">char</span> String[<span class="hljs-number">100</span>];				<span class="hljs-comment">//定义字符数组</span>
 va_list arg;					<span class="hljs-comment">//定义可变参数列表数据类型的变量arg</span>
 va_start(arg, format);			<span class="hljs-comment">//从format开始，接收参数列表到arg变量</span>
 <span class="hljs-built_in">vsprintf</span>(String, format, arg);	<span class="hljs-comment">//使用vsprintf打印格式化字符串和参数列表到字符数组中</span>
 va_end(arg);					<span class="hljs-comment">//结束变量arg</span>
 Serial_SendString(String);		<span class="hljs-comment">//串口发送字符数组（字符串）</span>
}

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：获取串口接收标志位</span>
<span class="hljs-comment">  * 参    数：无</span>
<span class="hljs-comment">  * 返 回 值：串口接收标志位，范围：0~1，接收到数据后，标志位置1，读取后标志位自动清零</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">uint8_t</span> <span class="hljs-title function_">Serial_GetRxFlag</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-keyword">if</span> (Serial_RxFlag == <span class="hljs-number">1</span>)			<span class="hljs-comment">//如果标志位为1</span>
 {
  Serial_RxFlag = <span class="hljs-number">0</span>;
  <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>;					<span class="hljs-comment">//则返回1，并自动清零标志位</span>
 }
 <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;						<span class="hljs-comment">//如果标志位为0，则返回0</span>
}

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：获取串口接收的数据</span>
<span class="hljs-comment">  * 参    数：无</span>
<span class="hljs-comment">  * 返 回 值：接收的数据，范围：0~255</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">uint8_t</span> <span class="hljs-title function_">Serial_GetRxData</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-keyword">return</span> Serial_RxData;			<span class="hljs-comment">//返回接收的数据变量</span>
}

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：USART1中断函数</span>
<span class="hljs-comment">  * 参    数：无</span>
<span class="hljs-comment">  * 返 回 值：无</span>
<span class="hljs-comment">  * 注意事项：此函数为中断函数，无需调用，中断触发后自动执行</span>
<span class="hljs-comment">  *           函数名为预留的指定名称，可以从启动文件复制</span>
<span class="hljs-comment">  *           请确保函数名正确，不能有任何差异，否则中断函数将不能进入</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">USART1_IRQHandler</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-keyword">if</span> (USART_GetITStatus(USART1, USART_IT_RXNE) == SET)		<span class="hljs-comment">//判断是否是USART1的接收事件触发的中断</span>
 {
  Serial_RxData = USART_ReceiveData(USART1);				<span class="hljs-comment">//读取数据寄存器，存放在接收的数据变量</span>
  Serial_RxFlag = <span class="hljs-number">1</span>;										<span class="hljs-comment">//置接收标志位变量为1</span>
  USART_ClearITPendingBit(USART1, USART_IT_RXNE);			<span class="hljs-comment">//清除USART1的RXNE标志位</span>
                <span class="hljs-comment">//读取数据寄存器会自动清除此标志位</span>
                <span class="hljs-comment">//如果已经读取了数据寄存器，也可以不执行此代码</span>
 }
}
</code></pre>



<h3 id="1-5-数据包接收"><a href="#1-5-数据包接收" class="headerlink" title="1.5 数据包接收"></a>1.5 数据包接收</h3><h4 id="1-5-1-数据模式"><a href="#1-5-1-数据模式" class="headerlink" title="1.5.1 数据模式"></a>1.5.1 数据模式</h4><ul>
<li>HEX模式/十六进制模式/二进制模式：以原始数据的形式显示</li>
<li>文本模式/字符模式：以原始数据编码后的形式显示</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/asc.png" alt="Ascll码表"></p>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/%E6%95%B0%E6%8D%AE%E6%A8%A1%E5%BC%8F.png" alt="数据模式"></p>
<h4 id="1-5-2-HEX数据包"><a href="#1-5-2-HEX数据包" class="headerlink" title="1.5.2 HEX数据包"></a>1.5.2 HEX数据包</h4><ul>
<li><strong>固定包长，含包头包尾</strong></li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706366291211.png" alt="固定包长"></p>
<ul>
<li><strong>可变包长，含包头包尾</strong></li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706366317290.png" alt="可变包长"></p>
<h4 id="1-5-3-文本数据包"><a href="#1-5-3-文本数据包" class="headerlink" title="1.5.3 文本数据包"></a>1.5.3 文本数据包</h4><ul>
<li><strong>固定包长，含包头包尾</strong></li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706366411492.png" alt="固定包长"></p>
<ul>
<li><strong>可变包长，含包头包尾</strong></li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706366433207.png" alt="可变包长"></p>
<h4 id="1-5-4-HEX数据包接收"><a href="#1-5-4-HEX数据包接收" class="headerlink" title="1.5.4 HEX数据包接收"></a>1.5.4 HEX数据包接收</h4><p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/%E6%8E%A5%E6%94%B6hex%E6%95%B0%E6%8D%AE%E5%8C%85.png" alt="接收hex数据包"></p>
<h4 id="1-5-5-文本数据包接收"><a href="#1-5-5-文本数据包接收" class="headerlink" title="1.5.5 文本数据包接收"></a>1.5.5 文本数据包接收</h4><p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/%E6%8E%A5%E6%94%B6%E6%96%87%E6%9C%AC%E6%95%B0%E6%8D%AE%E5%8C%85.png" alt="接收文本数据包"></p>
<h3 id="1-6-数据包代码示例"><a href="#1-6-数据包代码示例" class="headerlink" title="1.6 数据包代码示例"></a>1.6 数据包代码示例</h3><h4 id="1-6-1-HEX数据包"><a href="#1-6-1-HEX数据包" class="headerlink" title="1.6.1 HEX数据包"></a>1.6.1 HEX数据包</h4><pre><code class="hljs C"><span class="hljs-type">void</span> <span class="hljs-title function_">USART1_IRQHandler</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-type">static</span> <span class="hljs-type">uint8_t</span> RxState = <span class="hljs-number">0</span>;		<span class="hljs-comment">//定义表示当前状态机状态的静态变量</span>
 <span class="hljs-type">static</span> <span class="hljs-type">uint8_t</span> pRxPacket = <span class="hljs-number">0</span>;	<span class="hljs-comment">//定义表示当前接收数据位置的静态变量</span>
 <span class="hljs-keyword">if</span> (USART_GetITStatus(USART1, USART_IT_RXNE) == SET)		<span class="hljs-comment">//判断是否是USART1的接收事件触发的中断</span>
 {
  <span class="hljs-type">uint8_t</span> RxData = USART_ReceiveData(USART1);				<span class="hljs-comment">//读取数据寄存器，存放在接收的数据变量</span>
  
  <span class="hljs-comment">/*使用状态机的思路，依次处理数据包的不同部分*/</span>
  
  <span class="hljs-comment">/*当前状态为0，接收数据包包头*/</span>
  <span class="hljs-keyword">if</span> (RxState == <span class="hljs-number">0</span>)
  {
   <span class="hljs-keyword">if</span> (RxData == <span class="hljs-number">0xFF</span>)			<span class="hljs-comment">//如果数据确实是包头</span>
   {
    RxState = <span class="hljs-number">1</span>;			<span class="hljs-comment">//置下一个状态</span>
    pRxPacket = <span class="hljs-number">0</span>;			<span class="hljs-comment">//数据包的位置归零</span>
   }
  }
  <span class="hljs-comment">/*当前状态为1，接收数据包数据*/</span>
  <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (RxState == <span class="hljs-number">1</span>)
  {
   Serial_RxPacket[pRxPacket] = RxData;	<span class="hljs-comment">//将数据存入数据包数组的指定位置</span>
   pRxPacket ++;				<span class="hljs-comment">//数据包的位置自增</span>
   <span class="hljs-keyword">if</span> (pRxPacket &gt;= <span class="hljs-number">4</span>)			<span class="hljs-comment">//如果收够4个数据</span>
   {
    RxState = <span class="hljs-number">2</span>;			<span class="hljs-comment">//置下一个状态</span>
   }
  }
  <span class="hljs-comment">/*当前状态为2，接收数据包包尾*/</span>
  <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (RxState == <span class="hljs-number">2</span>)
  {
   <span class="hljs-keyword">if</span> (RxData == <span class="hljs-number">0xFE</span>)			<span class="hljs-comment">//如果数据确实是包尾部</span>
   {
    RxState = <span class="hljs-number">0</span>;			<span class="hljs-comment">//状态归0</span>
    Serial_RxFlag = <span class="hljs-number">1</span>;		<span class="hljs-comment">//接收数据包标志位置1，成功接收一个数据包</span>
   }
  }
  
  USART_ClearITPendingBit(USART1, USART_IT_RXNE);		<span class="hljs-comment">//清除标志位</span>
 }
}
</code></pre>

<h4 id="1-6-2-文本数据包"><a href="#1-6-2-文本数据包" class="headerlink" title="1.6.2 文本数据包"></a>1.6.2 文本数据包</h4><pre><code class="hljs C"><span class="hljs-type">void</span> <span class="hljs-title function_">USART1_IRQHandler</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-type">static</span> <span class="hljs-type">uint8_t</span> RxState = <span class="hljs-number">0</span>;		<span class="hljs-comment">//定义表示当前状态机状态的静态变量</span>
 <span class="hljs-type">static</span> <span class="hljs-type">uint8_t</span> pRxPacket = <span class="hljs-number">0</span>;	<span class="hljs-comment">//定义表示当前接收数据位置的静态变量</span>
 <span class="hljs-keyword">if</span> (USART_GetITStatus(USART1, USART_IT_RXNE) == SET)	<span class="hljs-comment">//判断是否是USART1的接收事件触发的中断</span>
 {
  <span class="hljs-type">uint8_t</span> RxData = USART_ReceiveData(USART1);			<span class="hljs-comment">//读取数据寄存器，存放在接收的数据变量</span>
  
  <span class="hljs-comment">/*使用状态机的思路，依次处理数据包的不同部分*/</span>
  
  <span class="hljs-comment">/*当前状态为0，接收数据包包头*/</span>
  <span class="hljs-keyword">if</span> (RxState == <span class="hljs-number">0</span>)
  {
   <span class="hljs-keyword">if</span> (RxData == <span class="hljs-string">'@'</span> &amp;&amp; Serial_RxFlag == <span class="hljs-number">0</span>)		<span class="hljs-comment">//如果数据确实是包头，并且上一个数据包已处理完毕</span>
   {
    RxState = <span class="hljs-number">1</span>;			<span class="hljs-comment">//置下一个状态</span>
    pRxPacket = <span class="hljs-number">0</span>;			<span class="hljs-comment">//数据包的位置归零</span>
   }
  }
  <span class="hljs-comment">/*当前状态为1，接收数据包数据，同时判断是否接收到了第一个包尾*/</span>
  <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (RxState == <span class="hljs-number">1</span>)
  {
   <span class="hljs-keyword">if</span> (RxData == <span class="hljs-string">'\r'</span>)			<span class="hljs-comment">//如果收到第一个包尾</span>
   {
    RxState = <span class="hljs-number">2</span>;			<span class="hljs-comment">//置下一个状态</span>
   }
   <span class="hljs-keyword">else</span>						<span class="hljs-comment">//接收到了正常的数据</span>
   {
    Serial_RxPacket[pRxPacket] = RxData;		<span class="hljs-comment">//将数据存入数据包数组的指定位置</span>
    pRxPacket ++;			<span class="hljs-comment">//数据包的位置自增</span>
   }
  }
  <span class="hljs-comment">/*当前状态为2，接收数据包第二个包尾*/</span>
  <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (RxState == <span class="hljs-number">2</span>)
  {
   <span class="hljs-keyword">if</span> (RxData == <span class="hljs-string">'\n'</span>)			<span class="hljs-comment">//如果收到第二个包尾</span>
   {
    RxState = <span class="hljs-number">0</span>;			<span class="hljs-comment">//状态归0</span>
    Serial_RxPacket[pRxPacket] = <span class="hljs-string">'\0'</span>;			<span class="hljs-comment">//将收到的字符数据包添加一个字符串结束标志</span>
    Serial_RxFlag = <span class="hljs-number">1</span>;		<span class="hljs-comment">//接收数据包标志位置1，成功接收一个数据包</span>
   }
  }
  
  USART_ClearITPendingBit(USART1, USART_IT_RXNE);		<span class="hljs-comment">//清除标志位</span>
 }
}</code></pre>

<h2 id="2-I2C通信"><a href="#2-I2C通信" class="headerlink" title="2. I2C通信"></a>2. I2C通信</h2><blockquote>
<p>STM32–I2C通信</p>
</blockquote>
<h3 id="2-1-I2C通信简介"><a href="#2-1-I2C通信简介" class="headerlink" title="2.1 I2C通信简介"></a>2.1 I2C通信简介</h3><ul>
<li>I2C（Inter IC Bus）是由Philips公司开发的一种通用数据总线</li>
<li>两根通信线：SCL（Serial Clock）、SDA（Serial Data）</li>
<li>同步，半双工</li>
<li>带数据应答</li>
<li>支持总线挂载多设备（一主多从、多主多从）</li>
</ul>
<h4 id="2-1-1-硬件电路"><a href="#2-1-1-硬件电路" class="headerlink" title="2.1.1 硬件电路"></a>2.1.1 硬件电路</h4><ul>
<li>所有I2C设备的SCL连在一起，SDA连在一起</li>
<li>设备的SCL和SDA均要配置成开漏输出模式</li>
<li>SCL和SDA各添加一个上拉电阻，阻值一般为4.7KΩ左右</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706263977088.png" alt="I2C挂载设备"></p>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706263989395.png" alt="电路"></p>
<h4 id="2-1-2-I2C时序基本单元"><a href="#2-1-2-I2C时序基本单元" class="headerlink" title="2.1.2 I2C时序基本单元"></a>2.1.2 I2C时序基本单元</h4><ul>
<li><strong>起始条件：</strong>SCL高电平期间，SDA从高电平切换到低电平</li>
<li><strong>终止条件：</strong>SCL高电平期间，SDA从低电平切换到高电平</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706264194346.png" alt="起始和终止条件"></p>
<ul>
<li><strong>发送一个字节：</strong>SCL低电平期间，主机将数据位依次放到SDA线上（高位先行），然后释放SCL，从机将在SCL高电平期间读取数据位，所以SCL高电平期间SDA不允许有数据变化，依次循环上述过程8次，即可发送一个字节</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706264284743.png" alt="发送一个字节"></p>
<ul>
<li><strong>接收一个字节：</strong>SCL低电平期间，从机将数据位依次放到SDA线上（高位先行），然后释放SCL，主机将在SCL高电平期间读取数据位，所以SCL高电平期间SDA不允许有数据变化，依次循环上述过程8次，即可接收一个字节（主机在接收之前，需要释放SDA）</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706264383307.png" alt="接收一个字节"></p>
<ul>
<li><strong>发送应答：</strong>主机在接收完一个字节之后，在下一个时钟发送一位数据，数据0表示应答，数据1表示非应答</li>
<li><strong>接收应答：</strong>主机在发送完一个字节之后，在下一个时钟接收一位数据，判断从机是否应答，数据0表示应答，数据1表示非应答（主机在接收之前，需要释放SDA）</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706264434067.png" alt="接收和发送回答"></p>
<h4 id="2-1-3-I2C时序"><a href="#2-1-3-I2C时序" class="headerlink" title="2.1.3 I2C时序"></a>2.1.3 I2C时序</h4><ul>
<li><strong>指定地址写</strong></li>
<li>对于指定设备（Slave Address），在指定地址（Reg Address）下，写入指定数据（Data）</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706264556589.png" alt="指定地址写"></p>
<ul>
<li><strong>当前地址读</strong></li>
<li>对于指定设备（Slave Address），在当前地址指针指示的地址下，读取从机数据（Data）</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706265129206.png" alt="当前地址写"></p>
<ul>
<li><strong>指定地址读</strong></li>
<li>对于指定设备（Slave Address），在指定地址（Reg Address）下，读取从机数据（Data）</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706265164365.png" alt="指定地址读"></p>
<h3 id="2-2-MPU6050简介"><a href="#2-2-MPU6050简介" class="headerlink" title="2.2 MPU6050简介"></a>2.2 MPU6050简介</h3><h4 id="2-2-1-陀螺仪介绍"><a href="#2-2-1-陀螺仪介绍" class="headerlink" title="2.2.1 陀螺仪介绍"></a>2.2.1 陀螺仪介绍</h4><ul>
<li>MPU6050是一个6轴姿态传感器，可以测量芯片自身X、Y、Z轴的加速度、角速度参数，通过数据融合，可进一步得到姿态角，常应用于平衡车、飞行器等需要检测自身姿态的场景</li>
<li>3轴加速度计（Accelerometer）：测量X、Y、Z轴的加速度</li>
<li>3轴陀螺仪传感器（Gyroscope）：测量X、Y、Z轴的角速度</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706265567624.png" alt="陀螺仪"></p>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706265575585.png" alt="加速度记"></p>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706265587929.png" alt="陀螺仪示意"></p>
<h4 id="2-2-2-MPU6050参数"><a href="#2-2-2-MPU6050参数" class="headerlink" title="2.2.2 MPU6050参数"></a>2.2.2 MPU6050参数</h4><ul>
<li>16位ADC采集传感器的模拟信号，量化范围：-32768~32767</li>
<li>加速度计满量程选择：±2、±4、±8、±16（g）</li>
<li>陀螺仪满量程选择： ±250、±500、±1000、±2000（°/sec）</li>
<li>可配置的数字低通滤波器</li>
<li>可配置的时钟源</li>
<li>可配置的采样分频</li>
<li>I2C从机地址：1101000（AD0=0） 1101001（AD0=1）</li>
</ul>
<h4 id="2-2-3-硬件电路"><a href="#2-2-3-硬件电路" class="headerlink" title="2.2.3 硬件电路"></a>2.2.3 硬件电路</h4><table>
<thead>
<tr>
<th><strong>引脚</strong></th>
<th><strong>功能</strong></th>
</tr>
</thead>
<tbody><tr>
<td>VCC、GND</td>
<td>电源</td>
</tr>
<tr>
<td>SCL、SDA</td>
<td>I2C通信引脚</td>
</tr>
<tr>
<td>XCL、XDA</td>
<td>主机I2C通信引脚</td>
</tr>
<tr>
<td>AD0</td>
<td>从机地址最低位</td>
</tr>
<tr>
<td>INT</td>
<td>中断信号输出</td>
</tr>
</tbody></table>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706265831995.png" alt="硬件电路"></p>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706265861339.png" alt="MPU6050内部寄存器"></p>
<h3 id="2-3-软件模拟I2C"><a href="#2-3-软件模拟I2C" class="headerlink" title="2.3 软件模拟I2C"></a>2.3 软件模拟I2C</h3><p>所谓软件模拟I2C即使用代码手动拉高拉低电平，实现I2C时序操作，可以随意选择SDA和SCL引脚使用更加灵活。</p>
<h4 id="2-3-1-I2C文件"><a href="#2-3-1-I2C文件" class="headerlink" title="2.3.1 I2C文件"></a>2.3.1 I2C文件</h4><p>创建I2C.c文件，编写I2C基本时序</p>
<pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"stm32f10x.h"</span>                  <span class="hljs-comment">// Device header</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"MyI2C.h"</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"Delay.h"</span></span>


<span class="hljs-type">void</span> <span class="hljs-title function_">MyI2C_W_SDA</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> BitValue)</span>
{
 GPIO_WriteBit(SDA_Port, SDA_Pin, (BitAction)BitValue);
 Delay_us(<span class="hljs-number">10</span>);
}

<span class="hljs-type">void</span> <span class="hljs-title function_">MyI2C_W_SCL</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> BitValue)</span>
{
 GPIO_WriteBit(SCL_Port, SCL_Pin, (BitAction)BitValue);
 Delay_us(<span class="hljs-number">10</span>);
}

<span class="hljs-type">uint8_t</span> <span class="hljs-title function_">MyI2C_R_SDA</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-type">uint8_t</span> BitValue;
 BitValue = GPIO_ReadInputDataBit(SDA_Port, SDA_Pin);
 Delay_us(<span class="hljs-number">10</span>);
 <span class="hljs-keyword">return</span> BitValue;
}


<span class="hljs-type">void</span> <span class="hljs-title function_">MyI2C_Init</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOB, ENABLE);
 GPIO_InitTypeDef GPIO_InitStructure;
 GPIO_InitStructure.GPIO_Mode = GPIO_Mode_Out_OD;
 GPIO_InitStructure.GPIO_Pin = SDA_Pin|SCL_Pin;
 GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
 
 GPIO_Init(SDA_Port, &amp;GPIO_InitStructure);
 GPIO_SetBits(SDA_Port, SDA_Pin|SCL_Pin);
}

<span class="hljs-type">void</span> <span class="hljs-title function_">MyI2C_Start</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-comment">//起始条件:SCL高电平期间,SDA从高电平切换到低电平</span>
 MyI2C_W_SDA(<span class="hljs-number">1</span>);
 MyI2C_W_SCL(<span class="hljs-number">1</span>);
 MyI2C_W_SDA(<span class="hljs-number">0</span>);
 <span class="hljs-comment">//SCL拉低,为--收发--数据做准备</span>
 MyI2C_W_SCL(<span class="hljs-number">0</span>);
}

<span class="hljs-type">void</span> <span class="hljs-title function_">MyI2C_Stop</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-comment">//终止条件:SCL高电平期间,SDA从低电平切换到高电平</span>
 MyI2C_W_SDA(<span class="hljs-number">0</span>);
 MyI2C_W_SCL(<span class="hljs-number">1</span>);
 MyI2C_W_SDA(<span class="hljs-number">1</span>);
}

<span class="hljs-type">void</span> <span class="hljs-title function_">MyI2C_SendByte</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> Byte)</span>
{
 <span class="hljs-type">uint8_t</span> i;
 <span class="hljs-keyword">for</span>(i=<span class="hljs-number">0</span>; i&lt;<span class="hljs-number">8</span>; i++)
 {
  <span class="hljs-comment">//第一种写法：</span>
  MyI2C_W_SDA(Byte &amp; (<span class="hljs-number">0x80</span> &gt;&gt; i));  <span class="hljs-comment">//0x80=1000 0000</span>
  <span class="hljs-comment">//第二种写法</span>
<span class="hljs-comment">//  if((Byte &amp; (0x80 &gt;&gt; i))==0) MyI2C_W_SDA(0);</span>
<span class="hljs-comment">//  else MyI2C_W_SDA(1);</span>
  <span class="hljs-comment">//拉高SCL读取数据</span>
  MyI2C_W_SCL(<span class="hljs-number">1</span>);
  <span class="hljs-comment">//拉低,让SDA放数据</span>
  MyI2C_W_SCL(<span class="hljs-number">0</span>);
 }
}

<span class="hljs-type">uint8_t</span> <span class="hljs-title function_">MyI2C_ReceiveByte</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-type">uint8_t</span> i, Byte=<span class="hljs-number">0x00</span>;
<span class="hljs-comment">// MyI2C_W_SCL(0);</span>
 MyI2C_W_SDA(<span class="hljs-number">1</span>);
 <span class="hljs-keyword">for</span>(i=<span class="hljs-number">0</span>; i&lt;<span class="hljs-number">8</span>; i++)
 {
  <span class="hljs-comment">//读取数据</span>
  MyI2C_W_SCL(<span class="hljs-number">1</span>);
  <span class="hljs-keyword">if</span>(MyI2C_R_SDA() == <span class="hljs-number">1</span>) {Byte |=(<span class="hljs-number">0x80</span> &gt;&gt; i);} <span class="hljs-comment">//0x00=0000 0000</span>
  MyI2C_W_SCL(<span class="hljs-number">0</span>);
 }	
 <span class="hljs-keyword">return</span> Byte;
}


<span class="hljs-type">void</span> <span class="hljs-title function_">MyI2C_SendAck</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> AckBit)</span>
{
 <span class="hljs-comment">//发送应答</span>
 MyI2C_W_SDA(AckBit);
 <span class="hljs-comment">//读取应答</span>
 MyI2C_W_SCL(<span class="hljs-number">1</span>);
 <span class="hljs-comment">//进入下一个时序</span>
 MyI2C_W_SCL(<span class="hljs-number">0</span>);
}

<span class="hljs-type">uint8_t</span> <span class="hljs-title function_">MyI2C_ReceiveAck</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-type">uint8_t</span> AckBit;
 <span class="hljs-comment">//发送应答</span>
 MyI2C_W_SDA(<span class="hljs-number">1</span>);
 <span class="hljs-comment">//读取应答</span>
 MyI2C_W_SCL(<span class="hljs-number">1</span>);
 AckBit = MyI2C_R_SDA();
 <span class="hljs-comment">//进入下一个时序</span>
 MyI2C_W_SCL(<span class="hljs-number">0</span>);
 <span class="hljs-keyword">return</span> AckBit;
}</code></pre>

<p>I2C.h文件定义引脚和函数声明</p>
<pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">ifndef</span> __MYI2C_H__</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> __MYI2C_H__</span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"stm32f10x.h"</span> </span>

<span class="hljs-meta">#<span class="hljs-keyword">define</span> SDA_Pin GPIO_Pin_11</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> SCL_Pin GPIO_Pin_10</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> SCL_Port GPIOB</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> SDA_Port GPIOB</span>

<span class="hljs-type">void</span> <span class="hljs-title function_">MyI2C_Init</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>;
<span class="hljs-type">uint8_t</span> <span class="hljs-title function_">MyI2C_R_SDA</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>;
<span class="hljs-type">void</span> <span class="hljs-title function_">MyI2C_W_SCL</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> BitValue)</span>;
<span class="hljs-type">void</span> <span class="hljs-title function_">MyI2C_W_SDA</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> BitValue)</span>;
<span class="hljs-type">void</span> <span class="hljs-title function_">MyI2C_Start</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>;
<span class="hljs-type">void</span> <span class="hljs-title function_">MyI2C_Stop</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>;
<span class="hljs-type">void</span> <span class="hljs-title function_">MyI2C_SendByte</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> Byte)</span>;
<span class="hljs-type">uint8_t</span> <span class="hljs-title function_">MyI2C_ReceiveByte</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>;
<span class="hljs-type">void</span> <span class="hljs-title function_">MyI2C_SendAck</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> AckBit)</span>;
<span class="hljs-type">uint8_t</span> <span class="hljs-title function_">MyI2C_ReceiveAck</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>;

<span class="hljs-meta">#<span class="hljs-keyword">endif</span></span></code></pre>

<h4 id="2-3-2-Mpu6050文件"><a href="#2-3-2-Mpu6050文件" class="headerlink" title="2.3.2 Mpu6050文件"></a>2.3.2 Mpu6050文件</h4><p>创建Mpu6050.c文件，编写MPU6050操作函数</p>
<pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"stm32f10x.h"</span>                  <span class="hljs-comment">// Device header</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"MyI2C.h"</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"Mpu6050.h"</span></span>

<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU_Address 0xD0  <span class="hljs-comment">//MPU6050的寻址 + R( Bit(0) )</span></span>

<span class="hljs-type">void</span> <span class="hljs-title function_">MPU6050_Init</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-comment">//初始化I2C引脚</span>
 MyI2C_Init();
 
 <span class="hljs-comment">//配置寄存器</span>
 MPU6050_WriteReg(MPU6050_PWR_MGMT_1, <span class="hljs-number">0x01</span>);
 MPU6050_WriteReg(MPU6050_PWR_MGMT_2, <span class="hljs-number">0x00</span>);
 MPU6050_WriteReg(MPU6050_SMPLRT_DIV, <span class="hljs-number">0x09</span>);
 MPU6050_WriteReg(MPU6050_CONFIG, <span class="hljs-number">0x06</span>);
 MPU6050_WriteReg(MPU6050_GYRO_CONFIG, <span class="hljs-number">0x18</span>);
 MPU6050_WriteReg(MPU6050_ACCEL_CONFIG, <span class="hljs-number">0x18</span>);
}


<span class="hljs-type">void</span> <span class="hljs-title function_">MPU6050_WriteReg</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> RegAddress, <span class="hljs-type">uint8_t</span> Byte)</span>
{
 MyI2C_Start();
 <span class="hljs-comment">//-1-指定从机的寻址 + R/W</span>
 MyI2C_SendByte(MPU_Address);
 <span class="hljs-comment">//接收应答</span>
 MyI2C_ReceiveAck();
 <span class="hljs-comment">//-2-指定要写入寄存器地址</span>
 MyI2C_SendByte(RegAddress);
 MyI2C_ReceiveAck();
 <span class="hljs-comment">//-3-发送数据(一个字节)</span>
 MyI2C_SendByte(Byte);
 MyI2C_ReceiveAck();
 MyI2C_Stop();
}


<span class="hljs-type">uint8_t</span> <span class="hljs-title function_">MPU6050_ReadReg</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> RegAddress)</span>
{
 <span class="hljs-type">uint8_t</span> Data;
 
 MyI2C_Start();
 MyI2C_SendByte(MPU_Address);
 MyI2C_ReceiveAck();
 MyI2C_SendByte(RegAddress);
 MyI2C_ReceiveAck();
 
 MyI2C_Start();
 MyI2C_SendByte(MPU_Address | <span class="hljs-number">0x01</span>);
 MyI2C_ReceiveAck();
 Data = MyI2C_ReceiveByte();
 MyI2C_SendAck(<span class="hljs-number">1</span>);
 MyI2C_Stop();
 
 <span class="hljs-keyword">return</span> Data;
}
<span class="hljs-type">uint8_t</span> <span class="hljs-title function_">MPU6050_GetID</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-keyword">return</span> MPU6050_ReadReg(MPU6050_WHO_AM_I);
}
<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：MPU6050获取数据</span>
<span class="hljs-comment">  * 参    数：AccX AccY AccZ 加速度计X、Y、Z轴的数据，使用输出参数的形式返回，范围：-32768~32767</span>
<span class="hljs-comment">  * 参    数：GyroX GyroY GyroZ 陀螺仪X、Y、Z轴的数据，使用输出参数的形式返回，范围：-32768~32767</span>
<span class="hljs-comment">  * 返 回 值：无</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">MPU6050_GetData</span><span class="hljs-params">(<span class="hljs-type">int16_t</span> *AccX, <span class="hljs-type">int16_t</span> *AccY, <span class="hljs-type">int16_t</span> *AccZ, </span>
<span class="hljs-params">      <span class="hljs-type">int16_t</span> *GyroX, <span class="hljs-type">int16_t</span> *GyroY, <span class="hljs-type">int16_t</span> *GyroZ)</span>
{
 <span class="hljs-type">uint8_t</span> DataH, DataL;								<span class="hljs-comment">//定义数据高8位和低8位的变量</span>
 
 DataH = MPU6050_ReadReg(MPU6050_ACCEL_XOUT_H);		<span class="hljs-comment">//读取加速度计X轴的高8位数据</span>
 DataL = MPU6050_ReadReg(MPU6050_ACCEL_XOUT_L);		<span class="hljs-comment">//读取加速度计X轴的低8位数据</span>
 *AccX = (DataH &lt;&lt; <span class="hljs-number">8</span>) | DataL;						<span class="hljs-comment">//数据拼接，通过输出参数返回</span>
 
 DataH = MPU6050_ReadReg(MPU6050_ACCEL_YOUT_H);		<span class="hljs-comment">//读取加速度计Y轴的高8位数据</span>
 DataL = MPU6050_ReadReg(MPU6050_ACCEL_YOUT_L);		<span class="hljs-comment">//读取加速度计Y轴的低8位数据</span>
 *AccY = (DataH &lt;&lt; <span class="hljs-number">8</span>) | DataL;						<span class="hljs-comment">//数据拼接，通过输出参数返回</span>
 
 DataH = MPU6050_ReadReg(MPU6050_ACCEL_ZOUT_H);		<span class="hljs-comment">//读取加速度计Z轴的高8位数据</span>
 DataL = MPU6050_ReadReg(MPU6050_ACCEL_ZOUT_L);		<span class="hljs-comment">//读取加速度计Z轴的低8位数据</span>
 *AccZ = (DataH &lt;&lt; <span class="hljs-number">8</span>) | DataL;						<span class="hljs-comment">//数据拼接，通过输出参数返回</span>
 
 DataH = MPU6050_ReadReg(MPU6050_GYRO_XOUT_H);		<span class="hljs-comment">//读取陀螺仪X轴的高8位数据</span>
 DataL = MPU6050_ReadReg(MPU6050_GYRO_XOUT_L);		<span class="hljs-comment">//读取陀螺仪X轴的低8位数据</span>
 *GyroX = (DataH &lt;&lt; <span class="hljs-number">8</span>) | DataL;						<span class="hljs-comment">//数据拼接，通过输出参数返回</span>
 
 DataH = MPU6050_ReadReg(MPU6050_GYRO_YOUT_H);		<span class="hljs-comment">//读取陀螺仪Y轴的高8位数据</span>
 DataL = MPU6050_ReadReg(MPU6050_GYRO_YOUT_L);		<span class="hljs-comment">//读取陀螺仪Y轴的低8位数据</span>
 *GyroY = (DataH &lt;&lt; <span class="hljs-number">8</span>) | DataL;						<span class="hljs-comment">//数据拼接，通过输出参数返回</span>
 
 DataH = MPU6050_ReadReg(MPU6050_GYRO_ZOUT_H);		<span class="hljs-comment">//读取陀螺仪Z轴的高8位数据</span>
 DataL = MPU6050_ReadReg(MPU6050_GYRO_ZOUT_L);		<span class="hljs-comment">//读取陀螺仪Z轴的低8位数据</span>
 *GyroZ = (DataH &lt;&lt; <span class="hljs-number">8</span>) | DataL;						<span class="hljs-comment">//数据拼接，通过输出参数返回</span>
}</code></pre>

<blockquote>
<p>MPU6050_GetData函数的使用方法见<a href="#2.3.3main%E4%B8%BB%E5%87%BD%E6%95%B0">主函数</a>代码，采用了指针做形参传递的方法。</p>
</blockquote>
<p>创建Mpu6050.h文件，定义MPU6050各个寄存器地址</p>
<pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">ifndef</span> __MPU6050_H__</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> __MPU6050_H__</span>
<span class="hljs-comment">//#include "stm32f10x.h" </span>

<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_SMPLRT_DIV		0x19</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_CONFIG			0x1A</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_GYRO_CONFIG		0x1B</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_ACCEL_CONFIG	0x1C</span>

<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_ACCEL_XOUT_H	0x3B</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_ACCEL_XOUT_L	0x3C</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_ACCEL_YOUT_H	0x3D</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_ACCEL_YOUT_L	0x3E</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_ACCEL_ZOUT_H	0x3F</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_ACCEL_ZOUT_L	0x40</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_TEMP_OUT_H		0x41</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_TEMP_OUT_L		0x42</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_GYRO_XOUT_H		0x43</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_GYRO_XOUT_L		0x44</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_GYRO_YOUT_H		0x45</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_GYRO_YOUT_L		0x46</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_GYRO_ZOUT_H		0x47</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_GYRO_ZOUT_L		0x48</span>

<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_PWR_MGMT_1		0x6B</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_PWR_MGMT_2		0x6C</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU6050_WHO_AM_I		0x75</span>

<span class="hljs-type">uint8_t</span> <span class="hljs-title function_">MPU6050_GetID</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>;
<span class="hljs-type">void</span> <span class="hljs-title function_">MPU6050_Init</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>;
<span class="hljs-type">void</span> <span class="hljs-title function_">MPU6050_WriteReg</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> RegAddress, <span class="hljs-type">uint8_t</span> Byte)</span>;
<span class="hljs-type">uint8_t</span> <span class="hljs-title function_">MPU6050_ReadReg</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> RegAddress)</span>;
<span class="hljs-type">void</span> <span class="hljs-title function_">MPU6050_GetData</span><span class="hljs-params">(<span class="hljs-type">int16_t</span> *AccX, <span class="hljs-type">int16_t</span> *AccY, <span class="hljs-type">int16_t</span> *AccZ, </span>
<span class="hljs-params">      <span class="hljs-type">int16_t</span> *GyroX, <span class="hljs-type">int16_t</span> *GyroY, <span class="hljs-type">int16_t</span> *GyroZ)</span>;

<span class="hljs-meta">#<span class="hljs-keyword">endif</span></span></code></pre>

<h4 id="2-3-3-main主函数"><a href="#2-3-3-main主函数" class="headerlink" title="2.3.3 main主函数"></a>2.3.3 main主函数</h4><pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"stm32f10x.h"</span>                  <span class="hljs-comment">// Device header</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"Delay.h"</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"OLED.h"</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"MPU6050.h"</span></span>

<span class="hljs-type">uint8_t</span> ID;        <span class="hljs-comment">//定义用于存放ID号的变量</span>
<span class="hljs-type">int16_t</span> AX, AY, AZ, GX, GY, GZ;   <span class="hljs-comment">//定义用于存放各个数据的变量</span>

<span class="hljs-type">int</span> <span class="hljs-title function_">main</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-comment">/*模块初始化*/</span>
 OLED_Init();		<span class="hljs-comment">//OLED初始化</span>
 MPU6050_Init();		<span class="hljs-comment">//MPU6050初始化</span>
 
 <span class="hljs-comment">/*显示ID号*/</span>
 OLED_ShowString(<span class="hljs-number">1</span>, <span class="hljs-number">1</span>, <span class="hljs-string">"ID:"</span>);		<span class="hljs-comment">//显示静态字符串</span>
 ID = MPU6050_GetID();				<span class="hljs-comment">//获取MPU6050的ID号</span>
 OLED_ShowHexNum(<span class="hljs-number">1</span>, <span class="hljs-number">4</span>, ID, <span class="hljs-number">2</span>);		<span class="hljs-comment">//OLED显示ID号</span>
 
 <span class="hljs-keyword">while</span> (<span class="hljs-number">1</span>)
 {
  MPU6050_GetData(&amp;AX, &amp;AY, &amp;AZ, &amp;GX, &amp;GY, &amp;GZ);		<span class="hljs-comment">//获取MPU6050的数据</span>
  OLED_ShowSignedNum(<span class="hljs-number">2</span>, <span class="hljs-number">1</span>, AX, <span class="hljs-number">5</span>);					<span class="hljs-comment">//OLED显示数据</span>
  OLED_ShowSignedNum(<span class="hljs-number">3</span>, <span class="hljs-number">1</span>, AY, <span class="hljs-number">5</span>);
  OLED_ShowSignedNum(<span class="hljs-number">4</span>, <span class="hljs-number">1</span>, AZ, <span class="hljs-number">5</span>);
  OLED_ShowSignedNum(<span class="hljs-number">2</span>, <span class="hljs-number">8</span>, GX, <span class="hljs-number">5</span>);
  OLED_ShowSignedNum(<span class="hljs-number">3</span>, <span class="hljs-number">8</span>, GY, <span class="hljs-number">5</span>);
  OLED_ShowSignedNum(<span class="hljs-number">4</span>, <span class="hljs-number">8</span>, GZ, <span class="hljs-number">5</span>);
 }
}</code></pre>



<h3 id="2-4-I2C硬件外设"><a href="#2-4-I2C硬件外设" class="headerlink" title="2.4 I2C硬件外设"></a>2.4 I2C硬件外设</h3><ul>
<li>STM32内部集成了硬件I2C收发电路，可以由硬件自动执行时钟生成、起始终止条件生成、应答位收发、数据收发等功能，减轻CPU的负担</li>
<li>支持多主机模型</li>
<li>支持7位/10位地址模式</li>
<li>支持不同的通讯速度，标准速度(高达100 kHz)，快速(高达400 kHz)</li>
<li>支持DMA</li>
<li>兼容SMBus协议</li>
<li>STM32F103C8T6 硬件I2C资源：I2C1、I2C2</li>
</ul>
<h4 id="2-4-1-I2C框图"><a href="#2-4-1-I2C框图" class="headerlink" title="2.4.1 I2C框图"></a>2.4.1 I2C框图</h4><p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706267084943.png" alt="I2C框图"></p>
<h4 id="2-4-2-I2C基本结构"><a href="#2-4-2-I2C基本结构" class="headerlink" title="2.4.2 I2C基本结构"></a>2.4.2 I2C基本结构</h4><p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706267260181.png" alt="I2C基本结构"></p>
<h4 id="2-4-3主机发送"><a href="#2-4-3主机发送" class="headerlink" title="2.4.3主机发送"></a>2.4.3主机发送</h4><p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706267294207.png" alt="主机发送"></p>
<h4 id="2-4-4-主机接收"><a href="#2-4-4-主机接收" class="headerlink" title="2.4.4 主机接收"></a>2.4.4 主机接收</h4><p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706267321796.png" alt="主机接收"></p>
<h3 id="2-5-硬件I2C"><a href="#2-5-硬件I2C" class="headerlink" title="2.5 硬件I2C"></a>2.5 硬件I2C</h3><p>保持I2C文件代码不编，修改Mpu6050.c的配置代码</p>
<pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"stm32f10x.h"</span>                  <span class="hljs-comment">// Device header</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"MyI2C.h"</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"Mpu6050.h"</span></span>

<span class="hljs-meta">#<span class="hljs-keyword">define</span> SDA_Pin GPIO_Pin_11</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> SCL_Pin GPIO_Pin_10</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> SCL_Port GPIOB</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> SDA_Port GPIOB</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> MPU_Address 0xD0  <span class="hljs-comment">//MPU6050的寻址 + R( Bit(0) )</span></span>

<span class="hljs-type">void</span> <span class="hljs-title function_">MPU6050_WaitEvent</span><span class="hljs-params">(I2C_TypeDef* I2Cx, <span class="hljs-type">uint32_t</span> I2C_EVENT)</span>
{
 <span class="hljs-type">uint32_t</span> Timeout;
 Timeout = <span class="hljs-number">10000</span>;
 <span class="hljs-keyword">while</span> (I2C_CheckEvent(I2Cx, I2C_EVENT) != SUCCESS)
 {
  Timeout --;
  <span class="hljs-keyword">if</span> (Timeout == <span class="hljs-number">0</span>)
  {
   <span class="hljs-keyword">break</span>;
  }
 }
}

<span class="hljs-type">void</span> <span class="hljs-title function_">MPU6050_Init</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-comment">//初始化I2C引脚</span>
 <span class="hljs-comment">//替换MyI2C_Init();</span>
 RCC_APB1PeriphClockCmd(RCC_APB1Periph_I2C2, ENABLE);
 RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOB, ENABLE);
 
 GPIO_InitTypeDef GPIO_InitStrcture;
 GPIO_InitStrcture.GPIO_Mode = GPIO_Mode_AF_OD;
 GPIO_InitStrcture.GPIO_Pin = SDA_Pin|SCL_Pin;
 GPIO_InitStrcture.GPIO_Speed = GPIO_Speed_50MHz;
 
 GPIO_Init(SDA_Port, &amp;GPIO_InitStrcture);
 
 I2C_InitTypeDef I2C_InitStructure;
 I2C_InitStructure.I2C_Ack = I2C_Ack_Enable;
 I2C_InitStructure.I2C_AcknowledgedAddress = I2C_AcknowledgedAddress_7bit;
 I2C_InitStructure.I2C_ClockSpeed = <span class="hljs-number">50000</span>;
 I2C_InitStructure.I2C_DutyCycle = I2C_DutyCycle_2;
 I2C_InitStructure.I2C_Mode = I2C_Mode_I2C;
 I2C_InitStructure.I2C_OwnAddress1 = <span class="hljs-number">0x00</span>;
 
 I2C_Init(I2C2, &amp;I2C_InitStructure);
 I2C_Cmd(I2C2, ENABLE); <span class="hljs-comment">//使能I2C2</span>
 
 <span class="hljs-comment">//配置寄存器</span>
 MPU6050_WriteReg(MPU6050_PWR_MGMT_1, <span class="hljs-number">0x01</span>);	<span class="hljs-comment">//电源管理寄存器1，取消睡眠模式，选择时钟源为X轴陀螺仪</span>
 MPU6050_WriteReg(MPU6050_PWR_MGMT_2, <span class="hljs-number">0x00</span>);	<span class="hljs-comment">//电源管理寄存器2，保持默认值0，所有轴均不待机</span>
 MPU6050_WriteReg(MPU6050_SMPLRT_DIV, <span class="hljs-number">0x09</span>);		<span class="hljs-comment">//采样率分频寄存器，配置采样率</span>
 MPU6050_WriteReg(MPU6050_CONFIG, <span class="hljs-number">0x06</span>);			<span class="hljs-comment">//配置寄存器，配置DLPF</span>
 MPU6050_WriteReg(MPU6050_GYRO_CONFIG, <span class="hljs-number">0x18</span>);	<span class="hljs-comment">//陀螺仪配置寄存器，选择满量程为±2000°/s</span>
 MPU6050_WriteReg(MPU6050_ACCEL_CONFIG, <span class="hljs-number">0x18</span>);	<span class="hljs-comment">//加速度计配置寄存器，选择满量程为±16g</span>
}


<span class="hljs-type">void</span> <span class="hljs-title function_">MPU6050_WriteReg</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> RegAddress, <span class="hljs-type">uint8_t</span> Byte)</span>
{
 <span class="hljs-comment">/*  软件I2C</span>
<span class="hljs-comment"> MyI2C_Start();</span>
<span class="hljs-comment"> //-1-指定从机的寻址 + R/W</span>
<span class="hljs-comment"> MyI2C_SendByte(MPU_Address);</span>
<span class="hljs-comment"> //接收应答</span>
<span class="hljs-comment"> MyI2C_ReceiveAck();</span>
<span class="hljs-comment"> //-2-指定要写入寄存器地址</span>
<span class="hljs-comment"> MyI2C_SendByte(RegAddress);</span>
<span class="hljs-comment"> MyI2C_ReceiveAck();</span>
<span class="hljs-comment"> //-3-发送数据(一个字节)</span>
<span class="hljs-comment"> MyI2C_SendByte(Byte);</span>
<span class="hljs-comment"> MyI2C_ReceiveAck();</span>
<span class="hljs-comment"> MyI2C_Stop();</span>
<span class="hljs-comment"> */</span>	
 <span class="hljs-comment">//硬件I2C</span>
 I2C_GenerateSTART(I2C2, ENABLE); 
 <span class="hljs-comment">//等待EV5事件（主机模式已选择）</span>
 MPU6050_WaitEvent(I2C2, I2C_EVENT_MASTER_MODE_SELECT);
 I2C_Send7bitAddress(I2C2, MPU_Address, I2C_Direction_Transmitter);
 <span class="hljs-comment">//等待EV6事件</span>
 MPU6050_WaitEvent(I2C2, I2C_EVENT_MASTER_TRANSMITTER_MODE_SELECTED);
 I2C_SendData(I2C2, RegAddress);
 <span class="hljs-comment">//等待EV8事件</span>
 MPU6050_WaitEvent(I2C2, I2C_EVENT_MASTER_BYTE_TRANSMITTING);
 I2C_SendData(I2C2, Byte);
 <span class="hljs-comment">//等待EV8-2事件</span>
 MPU6050_WaitEvent(I2C2, I2C_EVENT_MASTER_BYTE_TRANSMITTED);
 I2C_GenerateSTOP(I2C2, ENABLE);
}


<span class="hljs-type">uint8_t</span> <span class="hljs-title function_">MPU6050_ReadReg</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> RegAddress)</span>
{
 <span class="hljs-type">uint8_t</span> Data;
 <span class="hljs-comment">/* 软件I2C</span>
<span class="hljs-comment"> MyI2C_Start();</span>
<span class="hljs-comment"> MyI2C_SendByte(MPU_Address);</span>
<span class="hljs-comment"> MyI2C_ReceiveAck();</span>
<span class="hljs-comment"> MyI2C_SendByte(RegAddress);</span>
<span class="hljs-comment"> MyI2C_ReceiveAck();</span>
<span class="hljs-comment"> </span>
<span class="hljs-comment"> MyI2C_Start();</span>
<span class="hljs-comment"> MyI2C_SendByte(MPU_Address | 0x01);</span>
<span class="hljs-comment"> MyI2C_ReceiveAck();</span>
<span class="hljs-comment"> Data = MyI2C_ReceiveByte();</span>
<span class="hljs-comment"> MyI2C_SendAck(1);</span>
<span class="hljs-comment"> MyI2C_Stop();</span>
<span class="hljs-comment"> */</span>
 <span class="hljs-comment">//硬件I2C</span>
 I2C_GenerateSTART(I2C2, ENABLE); 
 <span class="hljs-comment">//等待EV5事件（主机模式已选择）</span>
 MPU6050_WaitEvent(I2C2, I2C_EVENT_MASTER_MODE_SELECT);
 I2C_Send7bitAddress(I2C2, MPU_Address, I2C_Direction_Transmitter);
 <span class="hljs-comment">//等待EV6事件</span>
 MPU6050_WaitEvent(I2C2, I2C_EVENT_MASTER_TRANSMITTER_MODE_SELECTED);
 I2C_SendData(I2C2, RegAddress);
 <span class="hljs-comment">//等待EV8-2事件</span>
 MPU6050_WaitEvent(I2C2, I2C_EVENT_MASTER_BYTE_TRANSMITTED);
 
 I2C_GenerateSTART(I2C2, ENABLE); 
 <span class="hljs-comment">//等待EV5事件（主机模式已选择）</span>
 MPU6050_WaitEvent(I2C2, I2C_EVENT_MASTER_MODE_SELECT);
 
 I2C_Send7bitAddress(I2C2, MPU_Address, I2C_Direction_Receiver);
 <span class="hljs-comment">//等待EV6事件</span>
 MPU6050_WaitEvent(I2C2, I2C_EVENT_MASTER_RECEIVER_MODE_SELECTED);
 <span class="hljs-comment">//提前将--应答位置0--产生停止条件</span>
 I2C_AcknowledgeConfig(I2C2, DISABLE);
 I2C_GenerateSTOP(I2C2, ENABLE);
 <span class="hljs-comment">//等待EV7事件</span>
 MPU6050_WaitEvent(I2C2, I2C_EVENT_MASTER_BYTE_RECEIVED);	
 
 Data = I2C_ReceiveData(I2C2);
 <span class="hljs-comment">//应答位置1</span>
 I2C_AcknowledgeConfig(I2C2, ENABLE);

 <span class="hljs-keyword">return</span> Data;
}
<span class="hljs-type">uint8_t</span> <span class="hljs-title function_">MPU6050_GetID</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-keyword">return</span> MPU6050_ReadReg(MPU6050_WHO_AM_I);
}
<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：MPU6050获取数据</span>
<span class="hljs-comment">  * 参    数：AccX AccY AccZ 加速度计X、Y、Z轴的数据，使用输出参数的形式返回，范围：-32768~32767</span>
<span class="hljs-comment">  * 参    数：GyroX GyroY GyroZ 陀螺仪X、Y、Z轴的数据，使用输出参数的形式返回，范围：-32768~32767</span>
<span class="hljs-comment">  * 返 回 值：无</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">MPU6050_GetData</span><span class="hljs-params">(<span class="hljs-type">int16_t</span> *AccX, <span class="hljs-type">int16_t</span> *AccY, <span class="hljs-type">int16_t</span> *AccZ, </span>
<span class="hljs-params">      <span class="hljs-type">int16_t</span> *GyroX, <span class="hljs-type">int16_t</span> *GyroY, <span class="hljs-type">int16_t</span> *GyroZ)</span>
{
 <span class="hljs-type">uint8_t</span> DataH, DataL;								<span class="hljs-comment">//定义数据高8位和低8位的变量</span>
 
 DataH = MPU6050_ReadReg(MPU6050_ACCEL_XOUT_H);		<span class="hljs-comment">//读取加速度计X轴的高8位数据</span>
 DataL = MPU6050_ReadReg(MPU6050_ACCEL_XOUT_L);		<span class="hljs-comment">//读取加速度计X轴的低8位数据</span>
 *AccX = (DataH &lt;&lt; <span class="hljs-number">8</span>) | DataL;						<span class="hljs-comment">//数据拼接，通过输出参数返回</span>
 
 DataH = MPU6050_ReadReg(MPU6050_ACCEL_YOUT_H);		<span class="hljs-comment">//读取加速度计Y轴的高8位数据</span>
 DataL = MPU6050_ReadReg(MPU6050_ACCEL_YOUT_L);		<span class="hljs-comment">//读取加速度计Y轴的低8位数据</span>
 *AccY = (DataH &lt;&lt; <span class="hljs-number">8</span>) | DataL;						<span class="hljs-comment">//数据拼接，通过输出参数返回</span>
 
 DataH = MPU6050_ReadReg(MPU6050_ACCEL_ZOUT_H);		<span class="hljs-comment">//读取加速度计Z轴的高8位数据</span>
 DataL = MPU6050_ReadReg(MPU6050_ACCEL_ZOUT_L);		<span class="hljs-comment">//读取加速度计Z轴的低8位数据</span>
 *AccZ = (DataH &lt;&lt; <span class="hljs-number">8</span>) | DataL;						<span class="hljs-comment">//数据拼接，通过输出参数返回</span>
 
 DataH = MPU6050_ReadReg(MPU6050_GYRO_XOUT_H);		<span class="hljs-comment">//读取陀螺仪X轴的高8位数据</span>
 DataL = MPU6050_ReadReg(MPU6050_GYRO_XOUT_L);		<span class="hljs-comment">//读取陀螺仪X轴的低8位数据</span>
 *GyroX = (DataH &lt;&lt; <span class="hljs-number">8</span>) | DataL;						<span class="hljs-comment">//数据拼接，通过输出参数返回</span>
 
 DataH = MPU6050_ReadReg(MPU6050_GYRO_YOUT_H);		<span class="hljs-comment">//读取陀螺仪Y轴的高8位数据</span>
 DataL = MPU6050_ReadReg(MPU6050_GYRO_YOUT_L);		<span class="hljs-comment">//读取陀螺仪Y轴的低8位数据</span>
 *GyroY = (DataH &lt;&lt; <span class="hljs-number">8</span>) | DataL;						<span class="hljs-comment">//数据拼接，通过输出参数返回</span>
 
 DataH = MPU6050_ReadReg(MPU6050_GYRO_ZOUT_H);		<span class="hljs-comment">//读取陀螺仪Z轴的高8位数据</span>
 DataL = MPU6050_ReadReg(MPU6050_GYRO_ZOUT_L);		<span class="hljs-comment">//读取陀螺仪Z轴的低8位数据</span>
 *GyroZ = (DataH &lt;&lt; <span class="hljs-number">8</span>) | DataL;						<span class="hljs-comment">//数据拼接，通过输出参数返回</span>
}
</code></pre>

<p>main主函数代码</p>
<pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"stm32f10x.h"</span>                  <span class="hljs-comment">// Device header</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"Delay.h"</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"OLED.h"</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"MPU6050.h"</span></span>

<span class="hljs-type">uint8_t</span> ID;        <span class="hljs-comment">//定义用于存放ID号的变量</span>
<span class="hljs-type">int16_t</span> AX, AY, AZ, GX, GY, GZ;   <span class="hljs-comment">//定义用于存放各个数据的变量</span>

<span class="hljs-type">int</span> <span class="hljs-title function_">main</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-comment">/*模块初始化*/</span>
 OLED_Init();		<span class="hljs-comment">//OLED初始化</span>
 MPU6050_Init();		<span class="hljs-comment">//MPU6050初始化</span>
 
 <span class="hljs-comment">/*显示ID号*/</span>
 OLED_ShowString(<span class="hljs-number">1</span>, <span class="hljs-number">1</span>, <span class="hljs-string">"ID:"</span>);		<span class="hljs-comment">//显示静态字符串</span>
 ID = MPU6050_GetID();				<span class="hljs-comment">//获取MPU6050的ID号</span>
 OLED_ShowHexNum(<span class="hljs-number">1</span>, <span class="hljs-number">4</span>, ID, <span class="hljs-number">2</span>);		<span class="hljs-comment">//OLED显示ID号</span>
 
 <span class="hljs-keyword">while</span> (<span class="hljs-number">1</span>)
 {
  MPU6050_GetData(&amp;AX, &amp;AY, &amp;AZ, &amp;GX, &amp;GY, &amp;GZ);		<span class="hljs-comment">//获取MPU6050的数据</span>
  OLED_ShowSignedNum(<span class="hljs-number">2</span>, <span class="hljs-number">1</span>, AX, <span class="hljs-number">5</span>);					<span class="hljs-comment">//OLED显示数据</span>
  OLED_ShowSignedNum(<span class="hljs-number">3</span>, <span class="hljs-number">1</span>, AY, <span class="hljs-number">5</span>);
  OLED_ShowSignedNum(<span class="hljs-number">4</span>, <span class="hljs-number">1</span>, AZ, <span class="hljs-number">5</span>);
  OLED_ShowSignedNum(<span class="hljs-number">2</span>, <span class="hljs-number">8</span>, GX, <span class="hljs-number">5</span>);
  OLED_ShowSignedNum(<span class="hljs-number">3</span>, <span class="hljs-number">8</span>, GY, <span class="hljs-number">5</span>);
  OLED_ShowSignedNum(<span class="hljs-number">4</span>, <span class="hljs-number">8</span>, GZ, <span class="hljs-number">5</span>);
 }
}

</code></pre>

<h2 id="3-SPI通信"><a href="#3-SPI通信" class="headerlink" title="3. SPI通信"></a>3. SPI通信</h2><blockquote>
<p>STM32–SPI通信</p>
</blockquote>
<h3 id="3-1-SPI通信简介"><a href="#3-1-SPI通信简介" class="headerlink" title="3.1 SPI通信简介"></a>3.1 SPI通信简介</h3><ul>
<li>SPI（Serial Peripheral Interface）是由Motorola公司开发的一种通用数据总线</li>
<li>四根通信线：SCK（Serial Clock）、MOSI（Master Output Slave Input）、MISO（Master Input Slave Output）、SS（Slave Select）–</li>
<li>同步，全双工</li>
<li>支持总线挂载多设备（一主多从，<strong>不支持</strong>多主机）</li>
</ul>
<h4 id="3-1-1-硬件电路"><a href="#3-1-1-硬件电路" class="headerlink" title="3.1.1 硬件电路"></a>3.1.1 硬件电路</h4><ul>
<li>所有SPI设备的SCK、MOSI、MISO分别连在一起</li>
<li>主机另外引出多条SS控制线，分别接到各从机的SS引脚</li>
<li>输出引脚配置为推挽输出，输入引脚配置为浮空或上拉输入</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706277356750.png" alt="SPI挂载设备"></p>
<h4 id="3-1-2-移位示意图"><a href="#3-1-2-移位示意图" class="headerlink" title="3.1.2 移位示意图"></a>3.1.2 移位示意图</h4><ul>
<li>高位先行，高电平移出数据，低电平移入</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706280036159.png" alt="移位示意图"></p>
<h4 id="3-1-3-SPI时基单元"><a href="#3-1-3-SPI时基单元" class="headerlink" title="3.1.3 SPI时基单元"></a>3.1.3 SPI时基单元</h4><ul>
<li><strong>起始条件：</strong>SS从高电平切换到低电平</li>
<li><strong>终止条件：</strong>SS从低电平切换到高电平</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706280340445.png" alt="起始和终止条件"></p>
<p>SPI交换字节有多种模式可以选择，主要取决于CPOL(时钟极性)和CPHA(时钟相位)，一个四种组合方式。</p>
<ul>
<li>交换一个字节<strong>（模式0）</strong></li>
<li>CPOL=0：空闲状态时，SCK为低电平</li>
<li>CPHA=0：SCK第一个边沿移入数据，第二个边沿移出数据</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706280519452.png" alt="模式0"></p>
<ul>
<li>交换一个字节<strong>（模式1）</strong></li>
<li>CPOL=0：空闲状态时，SCK为低电平</li>
<li>CPHA=1：SCK第一个边沿移出数据，第二个边沿移入数据</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706280584972.png" alt="模式1"></p>
<ul>
<li>交换一个字节<strong>（模式2）</strong></li>
<li>CPOL=1：空闲状态时，SCK为高电平</li>
<li>CPHA=0：SCK第一个边沿移入数据，第二个边沿移出数据</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706280625411.png" alt="模式2"></p>
<ul>
<li>交换一个字节<strong>（模式3）</strong></li>
<li>CPOL=1：空闲状态时，SCK为高电平</li>
<li>CPHA=1：SCK第一个边沿移出数据，第二个边沿移入数据</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706280660766.png" alt="模式3"></p>
<h4 id="3-1-4-SPI时序"><a href="#3-1-4-SPI时序" class="headerlink" title="3.1.4 SPI时序"></a>3.1.4 SPI时序</h4><ul>
<li><strong>发送指令</strong></li>
<li>向SS指定的设备，发送指令（0x06）</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706280807159.png" alt="发送指令"></p>
<ul>
<li>指定地址写</li>
<li>向SS指定的设备，发送写指令（0x02），随后在指定地址（Address[23:0]）下，写入指定数据（Data）</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706280904218.png" alt="指定地址写1"></p>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706280941789.png" alt="指定地址写2"></p>
<ul>
<li>指定地址读</li>
<li>向SS指定的设备，发送读指令（0x03），  随后在指定地址（Address[23:0]）下，读取从机数据（Data）</li>
</ul>
<blockquote>
<p>与上图类似，将最后一步指令发送数据改为接收数据即可</p>
</blockquote>
<h3 id="3-2-W25Q64简介"><a href="#3-2-W25Q64简介" class="headerlink" title="3.2 W25Q64简介"></a>3.2 W25Q64简介</h3><ul>
<li><p>W25Qxx系列是一种低成本、小型化、使用简单的非易失性存储器，常应用于数据存储、字库存储、固件程序存储等场景</p>
</li>
<li><p>存储介质：Nor Flash（闪存）</p>
</li>
<li><p>时钟频率：80MHz / 160MHz (Dual SPI) / 320MHz (Quad SPI)</p>
</li>
<li><p>存储容量（24位地址）：</p>
<p>W25Q40：    4Mbit / 512KByte</p>
<p>W25Q80：    8Mbit / 1MByte</p>
<p>W25Q16：    16Mbit / 2MByte</p>
<p>W25Q32：    32Mbit / 4MByte</p>
<p>W25Q64：    64Mbit / 8MByte</p>
<p>W25Q128：  128Mbit / 16MByte</p>
<p>W25Q256：  256Mbit / 32MByte</p>
</li>
</ul>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706281198028.png" alt="W25Q64"></p>
<h4 id="3-2-1-硬件电路"><a href="#3-2-1-硬件电路" class="headerlink" title="3.2.1 硬件电路"></a>3.2.1 硬件电路</h4><p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706281226510.png" alt="硬件电路"></p>
<p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706281236623.png" alt="引脚"></p>
<table>
<thead>
<tr>
<th><strong>引脚</strong></th>
<th><strong>功能</strong></th>
</tr>
</thead>
<tbody><tr>
<td>VCC、GND</td>
<td>电源（2.7~3.6V）</td>
</tr>
<tr>
<td>CS（SS）</td>
<td>SPI片选</td>
</tr>
<tr>
<td>CLK（SCK）</td>
<td>SPI时钟</td>
</tr>
<tr>
<td>DI（MOSI）</td>
<td>SPI主机输出从机输入</td>
</tr>
<tr>
<td>DO（MISO）</td>
<td>SPI主机输入从机输出</td>
</tr>
<tr>
<td>WP</td>
<td>写保护</td>
</tr>
<tr>
<td>HOLD</td>
<td>数据保持</td>
</tr>
</tbody></table>
<h4 id="3-2-2-W25Q64框图"><a href="#3-2-2-W25Q64框图" class="headerlink" title="3.2.2 W25Q64框图"></a>3.2.2 W25Q64框图</h4><p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706281271396.png" alt="W25Q64框图"></p>
<h4 id="3-2-3-Flash读写注意事项"><a href="#3-2-3-Flash读写注意事项" class="headerlink" title="3.2.3 Flash读写注意事项"></a>3.2.3 Flash读写注意事项</h4><p><strong>写入操作时：</strong></p>
<ul>
<li>写入操作前，必须先进行写使能</li>
<li>每个数据位只能由1改写为0，不能由0改写为1</li>
<li>写入数据前必须先擦除，擦除后，所有数据位变为1</li>
<li>擦除必须按最小擦除单元进行</li>
<li>连续写入多字节时，最多写入一页的数据，超过页尾位置的数据，会回到页首覆盖写入</li>
<li>写入操作结束后，芯片进入忙状态，不响应新的读写操作</li>
</ul>
<p><strong>读取操作时：</strong></p>
<ul>
<li>直接调用读取时序，无需使能，无需额外操作，没有页的限制，读取操作结束后不会进入忙状态，但不能在忙状态时读取</li>
</ul>
<h3 id="3-3-软件模拟SPI"><a href="#3-3-软件模拟SPI" class="headerlink" title="3.3 软件模拟SPI"></a>3.3 软件模拟SPI</h3><h4 id="3-3-1-SPI文件"><a href="#3-3-1-SPI文件" class="headerlink" title="3.3.1 SPI文件"></a>3.3.1 SPI文件</h4><p>创建MySPI.c文件，编写SPI基本时序单元</p>
<pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"stm32f10x.h"</span>                  <span class="hljs-comment">// Device header</span></span>

<span class="hljs-type">void</span> <span class="hljs-title function_">MySPI_W_SS</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> BitValue)</span>
{
 GPIO_WriteBit(GPIOA, GPIO_Pin_4, (BitAction)BitValue);
}

<span class="hljs-type">void</span> <span class="hljs-title function_">MySPI_W_MOSI</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> BitValue)</span>
{
 GPIO_WriteBit(GPIOA, GPIO_Pin_7, (BitAction)BitValue);
}

<span class="hljs-type">void</span> <span class="hljs-title function_">MySPI_W_CLK</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> BitValue)</span>
{
 GPIO_WriteBit(GPIOA, GPIO_Pin_5, (BitAction)BitValue);
}

<span class="hljs-type">uint8_t</span> <span class="hljs-title function_">MySPI_R_MISO</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-keyword">return</span> GPIO_ReadInputDataBit(GPIOA, GPIO_Pin_6);
}


<span class="hljs-comment">//主机输出引脚：SCK、MOSI、SS(复用推挽输出模式);主机输入引脚：MISO(上拉输入模式)</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">MySPI_Init</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA, ENABLE);
 
 GPIO_InitTypeDef GPIO_InitStructure;
 GPIO_InitStructure.GPIO_Mode = GPIO_Mode_Out_PP;
 GPIO_InitStructure.GPIO_Pin = GPIO_Pin_4|GPIO_Pin_5|GPIO_Pin_7;
 GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
 GPIO_Init(GPIOA, &amp;GPIO_InitStructure);
 
 GPIO_InitStructure.GPIO_Mode = GPIO_Mode_IPU;
 GPIO_InitStructure.GPIO_Pin = GPIO_Pin_6;
 GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
 GPIO_Init(GPIOA, &amp;GPIO_InitStructure);
 
 <span class="hljs-comment">//初始化引脚电压(SPI0模式)</span>
 MySPI_W_SS(<span class="hljs-number">1</span>);
 MySPI_W_CLK(<span class="hljs-number">0</span>);
}

<span class="hljs-type">void</span> <span class="hljs-title function_">MySPI_Start</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 MySPI_W_SS(<span class="hljs-number">0</span>);				<span class="hljs-comment">//拉低SS，开始时序</span>
}
<span class="hljs-type">void</span> <span class="hljs-title function_">MySPI_Stop</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 MySPI_W_SS(<span class="hljs-number">1</span>);				<span class="hljs-comment">//拉高SS，终止时序</span>
}

<span class="hljs-comment">//</span>
<span class="hljs-type">uint8_t</span> <span class="hljs-title function_">MySPI_SwapByte</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> ByteSend)</span>
{
 <span class="hljs-type">uint8_t</span> i,ByteReceive=<span class="hljs-number">0x00</span>;
 <span class="hljs-keyword">for</span>(i=<span class="hljs-number">0</span>;i&lt;<span class="hljs-number">8</span>;i++)
 {
  <span class="hljs-comment">//移出数据</span>
  MySPI_W_MOSI(ByteSend &amp; (<span class="hljs-number">0x80</span>&gt;&gt;i));
  MySPI_W_CLK(<span class="hljs-number">1</span>);
  <span class="hljs-comment">//移入数据</span>
  <span class="hljs-comment">//当MISO为1时，置变量指定位为1，当MISO为0时，不做处理，指定位为默认的初值0</span>
  <span class="hljs-keyword">if</span>(MySPI_R_MISO()==<span class="hljs-number">1</span>){ByteReceive |= (<span class="hljs-number">0x80</span>&gt;&gt;i);}
  MySPI_W_CLK(<span class="hljs-number">0</span>);
 }
 <span class="hljs-keyword">return</span> ByteReceive;
} 
</code></pre>

<h4 id="3-3-2-W25Q64文件"><a href="#3-3-2-W25Q64文件" class="headerlink" title="3.3.2 W25Q64文件"></a>3.3.2 W25Q64文件</h4><p>创建W25Q64.c文件，编写W25Q64操作函数</p>
<pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"stm32f10x.h"</span>                  <span class="hljs-comment">// Device header</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"MySPI.h"</span></span>
<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"W25Q64.h"</span></span>

<span class="hljs-type">void</span> <span class="hljs-title function_">W25Q64_Init</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 MySPI_Init();
}

<span class="hljs-type">void</span> <span class="hljs-title function_">W25Q64_GetID</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> *MID, <span class="hljs-type">uint16_t</span> *DID)</span>
{
 MySPI_Start();
 MySPI_SwapByte(W25Q64_JEDEC_ID);
 *MID = MySPI_SwapByte(<span class="hljs-number">0xFF</span>);
 *DID = MySPI_SwapByte(<span class="hljs-number">0xFF</span>);
 <span class="hljs-comment">//左移8位,后续获取底八位</span>
 *DID&lt;&lt;=<span class="hljs-number">8</span>;
 <span class="hljs-comment">//取或“|”</span>
 *DID |= MySPI_SwapByte(<span class="hljs-number">0xFF</span>);
 MySPI_Stop();
}

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * @brief  W25Q64写使能</span>
<span class="hljs-comment">  * @param  无</span>
<span class="hljs-comment">  * @retval 无</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">W25Q64_WriteEnable</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 MySPI_Start();
 MySPI_SwapByte(W25Q64_WRITE_ENABLE);
 MySPI_Stop();
}

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * @brief  W25Q64等待“忙”状态</span>
<span class="hljs-comment">  * @param  无</span>
<span class="hljs-comment">  * @retval 无</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">W25Q64_WaitBusy</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-type">uint32_t</span> Timeout;
 MySPI_Start();
 <span class="hljs-comment">//读状态寄存器1的最后一位（1为忙）</span>
 MySPI_SwapByte(W25Q64_READ_STATUS_REGISTER_1);
 <span class="hljs-keyword">while</span> ((MySPI_SwapByte(W25Q64_DUMMY_BYTE) &amp; <span class="hljs-number">0x01</span>)==<span class="hljs-number">1</span>);
 
 Timeout = <span class="hljs-number">100000</span>;							<span class="hljs-comment">//给定超时计数时间</span>
 <span class="hljs-keyword">while</span> ((MySPI_SwapByte(W25Q64_DUMMY_BYTE) &amp; <span class="hljs-number">0x01</span>) == <span class="hljs-number">0x01</span>)	<span class="hljs-comment">//循环等待忙标志位</span>
 {
  Timeout --;								<span class="hljs-comment">//等待时，计数值自减</span>
  <span class="hljs-keyword">if</span> (Timeout == <span class="hljs-number">0</span>)						<span class="hljs-comment">//自减到0后，等待超时</span>
  {
   <span class="hljs-comment">/*超时的错误处理代码，可以添加到此处*/</span>
   <span class="hljs-keyword">break</span>;								<span class="hljs-comment">//跳出等待，不等了</span>
  }
 }
 
 MySPI_Stop();
}
<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：W25Q64页编程</span>
<span class="hljs-comment">  * 参    数：Address 页编程的起始地址，范围：0x000000~0x7FFFFF</span>
<span class="hljs-comment">  * 参    数：DataArray 用于写入数据的数组</span>
<span class="hljs-comment">  * 参    数：Count 要写入数据的数量，范围：0~256</span>
<span class="hljs-comment">  * 返 回 值：无</span>
<span class="hljs-comment">  * 注意事项：写入的地址范围不能跨页</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">W25Q64_PageProgram</span><span class="hljs-params">(<span class="hljs-type">uint32_t</span> Address, <span class="hljs-type">uint8_t</span> *DataArray, <span class="hljs-type">uint8_t</span> Count)</span>
{
 <span class="hljs-type">uint32_t</span> i;
 W25Q64_WriteEnable();
 MySPI_Start();
 MySPI_SwapByte(W25Q64_PAGE_PROGRAM);
 MySPI_SwapByte(Address&gt;&gt;<span class="hljs-number">16</span>);
 MySPI_SwapByte(Address&gt;&gt;<span class="hljs-number">8</span>);
 MySPI_SwapByte(Address);
 <span class="hljs-keyword">for</span>(i=<span class="hljs-number">0</span>; i&lt;Count; i++)
 {
  MySPI_SwapByte(DataArray[i]);
 }
 
 MySPI_Stop();
 W25Q64_WaitBusy();
}

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：W25Q64扇区擦除（4KB）</span>
<span class="hljs-comment">  * 参    数：Address 指定扇区的地址，范围：0x000000~0x7FFFFF</span>
<span class="hljs-comment">  * 返 回 值：无</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">W25Q64_SectorErase</span><span class="hljs-params">(<span class="hljs-type">uint32_t</span> Address)</span>
{ 
 W25Q64_WriteEnable();
 MySPI_Start();

 MySPI_SwapByte(W25Q64_SECTOR_ERASE_4KB);
 MySPI_SwapByte(Address&gt;&gt;<span class="hljs-number">16</span>);
 MySPI_SwapByte(Address&gt;&gt;<span class="hljs-number">8</span>);
 MySPI_SwapByte(Address);

 MySPI_Stop();
 W25Q64_WaitBusy();
}

<span class="hljs-comment">/**</span>
<span class="hljs-comment">  * 函    数：W25Q64读取数据</span>
<span class="hljs-comment">  * 参    数：Address 读取数据的起始地址，范围：0x000000~0x7FFFFF</span>
<span class="hljs-comment">  * 参    数：DataArray 用于接收读取数据的数组，通过输出参数返回</span>
<span class="hljs-comment">  * 参    数：Count 要读取数据的数量，范围：0~0x800000</span>
<span class="hljs-comment">  * 返 回 值：无</span>
<span class="hljs-comment">  */</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">W25Q64_ReadData</span><span class="hljs-params">(<span class="hljs-type">uint32_t</span> Address, <span class="hljs-type">uint8_t</span> *DataArray, <span class="hljs-type">uint8_t</span> Count)</span>
{
 <span class="hljs-type">uint32_t</span> i;
 MySPI_Start();
 MySPI_SwapByte(W25Q64_READ_DATA);
 MySPI_SwapByte(Address&gt;&gt;<span class="hljs-number">16</span>);
 MySPI_SwapByte(Address&gt;&gt;<span class="hljs-number">8</span>);
 MySPI_SwapByte(Address);
 <span class="hljs-keyword">for</span>(i=<span class="hljs-number">0</span>; i&lt;Count; i++)
 {
  DataArray[i] = MySPI_SwapByte(W25Q64_DUMMY_BYTE);
 }
 
 MySPI_Stop();
}

</code></pre>

<p>在W25Q64.h文件，记录W25Q64寄存器地址</p>
<pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">ifndef</span> __W25Q64_H__</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> __W25Q64_H__</span>

<span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"stm32f10x.h"</span> </span>

<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_WRITE_ENABLE       0x06</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_WRITE_DISABLE      0x04</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_READ_STATUS_REGISTER_1    0x05</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_READ_STATUS_REGISTER_2    0x35</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_WRITE_STATUS_REGISTER    0x01</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_PAGE_PROGRAM       0x02</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_QUAD_PAGE_PROGRAM     0x32</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_BLOCK_ERASE_64KB      0xD8</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_BLOCK_ERASE_32KB      0x52</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_SECTOR_ERASE_4KB      0x20</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_CHIP_ERASE       0xC7</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_ERASE_SUSPEND      0x75</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_ERASE_RESUME       0x7A</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_POWER_DOWN       0xB9</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_HIGH_PERFORMANCE_MODE    0xA3</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_CONTINUOUS_READ_MODE_RESET   0xFF</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_RELEASE_POWER_DOWN_HPM_DEVICE_ID  0xAB</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_MANUFACTURER_DEVICE_ID    0x90</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_READ_UNIQUE_ID      0x4B</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_JEDEC_ID        0x9F</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_READ_DATA       0x03</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_FAST_READ       0x0B</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_FAST_READ_DUAL_OUTPUT    0x3B</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_FAST_READ_DUAL_IO     0xBB</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_FAST_READ_QUAD_OUTPUT    0x6B</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_FAST_READ_QUAD_IO     0xEB</span>
<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_OCTAL_WORD_READ_QUAD_IO    0xE3</span>

<span class="hljs-meta">#<span class="hljs-keyword">define</span> W25Q64_DUMMY_BYTE       0xFF</span>


<span class="hljs-type">void</span> <span class="hljs-title function_">W25Q64_Init</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>;
<span class="hljs-type">void</span> <span class="hljs-title function_">W25Q64_GetID</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> *MID, <span class="hljs-type">uint16_t</span> *DID)</span>;
<span class="hljs-type">void</span> <span class="hljs-title function_">W25Q64_SectorErase</span><span class="hljs-params">(<span class="hljs-type">uint32_t</span> Address)</span>;
<span class="hljs-type">void</span> <span class="hljs-title function_">W25Q64_ReadData</span><span class="hljs-params">(<span class="hljs-type">uint32_t</span> Address, <span class="hljs-type">uint8_t</span> *DataArray, <span class="hljs-type">uint8_t</span> Count)</span>;
<span class="hljs-type">void</span> <span class="hljs-title function_">W25Q64_PageProgram</span><span class="hljs-params">(<span class="hljs-type">uint32_t</span> Address, <span class="hljs-type">uint8_t</span> *DataArray, <span class="hljs-type">uint8_t</span> Count)</span>;

<span class="hljs-meta">#<span class="hljs-keyword">endif</span></span>

</code></pre>

<h3 id="3-4-SPI硬件外设"><a href="#3-4-SPI硬件外设" class="headerlink" title="3.4 SPI硬件外设"></a>3.4 SPI硬件外设</h3><ul>
<li>STM32内部集成了硬件SPI收发电路，可以由硬件自动执行时钟生成、数据收发等功能，减轻CPU的负担</li>
<li>可配置8位/16位数据帧、高位先行/低位先行</li>
<li>时钟频率： fPCLK / (2, 4, 8, 16, 32, 64, 128, 256)</li>
<li>支持多主机模型、主或从操作</li>
<li>可精简为半双工/单工通信</li>
<li>支持DMA</li>
<li>兼容I2S协议</li>
<li>STM32F103C8T6 硬件SPI资源：SPI1、SPI2</li>
</ul>
<h4 id="3-4-1-SPI框图"><a href="#3-4-1-SPI框图" class="headerlink" title="3.4.1 SPI框图"></a>3.4.1 SPI框图</h4><p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706282048455.png" alt="SPI框图"></p>
<h4 id="3-4-2-SPI基本结构"><a href="#3-4-2-SPI基本结构" class="headerlink" title="3.4.2 SPI基本结构"></a>3.4.2 SPI基本结构</h4><p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706282079465.png" alt="SPI基本结构"></p>
<h4 id="3-4-3-主模式全双工连续传输"><a href="#3-4-3-主模式全双工连续传输" class="headerlink" title="3.4.3 主模式全双工连续传输"></a>3.4.3 主模式全双工连续传输</h4><p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706282124802.png" alt="主模式全双工连续传输"></p>
<h4 id="3-4-4-非连续传输"><a href="#3-4-4-非连续传输" class="headerlink" title="3.4.4 非连续传输"></a>3.4.4 非连续传输</h4><p><img src="/2024/01/30/STM32%E5%9B%9B%E5%A4%A7%E9%80%9A%E4%BF%A1%E5%8D%8F%E8%AE%AE/1706282162689.png" alt="非连续传输"></p>
<h3 id="3-5-硬件SPI"><a href="#3-5-硬件SPI" class="headerlink" title="3.5 硬件SPI"></a>3.5 硬件SPI</h3><p>修改MySPI.c文件</p>
<pre><code class="hljs c"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">"stm32f10x.h"</span>                  <span class="hljs-comment">// Device header</span></span>

<span class="hljs-type">void</span> <span class="hljs-title function_">MySPI_W_SS</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> BitValue)</span>
{
 GPIO_WriteBit(GPIOA, GPIO_Pin_4, (BitAction)BitValue);
}

<span class="hljs-type">void</span> <span class="hljs-title function_">MySPI_W_MOSI</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> BitValue)</span>
{
 GPIO_WriteBit(GPIOA, GPIO_Pin_7, (BitAction)BitValue);
}

<span class="hljs-type">void</span> <span class="hljs-title function_">MySPI_W_CLK</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> BitValue)</span>
{
 GPIO_WriteBit(GPIOA, GPIO_Pin_5, (BitAction)BitValue);
}

<span class="hljs-type">uint8_t</span> <span class="hljs-title function_">MySPI_R_MISO</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 <span class="hljs-keyword">return</span> GPIO_ReadInputDataBit(GPIOA, GPIO_Pin_6);
}


<span class="hljs-comment">//主机输出引脚：SCK、MOSI、SS(复用推挽输出模式);主机输入引脚：MISO(上拉输入模式)</span>
<span class="hljs-type">void</span> <span class="hljs-title function_">MySPI_Init</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 RCC_APB2PeriphClockCmd(RCC_APB2Periph_GPIOA, ENABLE);
 RCC_APB2PeriphClockCmd(RCC_APB2Periph_SPI1, ENABLE);

 GPIO_InitTypeDef GPIO_InitStructure;
 GPIO_InitStructure.GPIO_Mode = GPIO_Mode_Out_PP;
 GPIO_InitStructure.GPIO_Pin = GPIO_Pin_4;
 GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
 GPIO_Init(GPIOA, &amp;GPIO_InitStructure);
 
 GPIO_InitStructure.GPIO_Mode = GPIO_Mode_IPU;
 GPIO_InitStructure.GPIO_Pin = GPIO_Pin_6;
 GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
 GPIO_Init(GPIOA, &amp;GPIO_InitStructure);
 
 GPIO_InitStructure.GPIO_Mode = GPIO_Mode_AF_PP;
 GPIO_InitStructure.GPIO_Pin = GPIO_Pin_5|GPIO_Pin_7;
 GPIO_InitStructure.GPIO_Speed = GPIO_Speed_50MHz;
 GPIO_Init(GPIOA, &amp;GPIO_InitStructure);
 
<span class="hljs-comment">// //初始化引脚电压(SPI0模式)</span>
<span class="hljs-comment">// MySPI_W_SS(1);</span>
<span class="hljs-comment">// MySPI_W_CLK(0);</span>
 SPI_InitTypeDef SPI_InitStructure;
 SPI_InitStructure.SPI_Mode = SPI_Mode_Master;  <span class="hljs-comment">//主机模式</span>
 SPI_InitStructure.SPI_BaudRatePrescaler = SPI_BaudRatePrescaler_128;  <span class="hljs-comment">//分频系数</span>
 SPI_InitStructure.SPI_CPHA = SPI_CPHA_1Edge; <span class="hljs-comment">//CPHA=0</span>
 SPI_InitStructure.SPI_CPOL = SPI_CPOL_Low;  <span class="hljs-comment">//CPOL=0,SPI模式0</span>
 SPI_InitStructure.SPI_CRCPolynomial = <span class="hljs-number">7</span>;
 SPI_InitStructure.SPI_DataSize = SPI_DataSize_8b; <span class="hljs-comment">//8位数据帧</span>
 SPI_InitStructure.SPI_Direction = SPI_Direction_2Lines_FullDuplex; <span class="hljs-comment">//全双工</span>
 SPI_InitStructure.SPI_FirstBit = SPI_FirstBit_MSB; <span class="hljs-comment">//高位先行</span>
 SPI_InitStructure.SPI_NSS = SPI_NSS_Soft; <span class="hljs-comment">//软件模拟SS引脚</span>
 
 SPI_Init(SPI1, &amp;SPI_InitStructure);
 SPI_Cmd(SPI1, ENABLE);
 MySPI_W_SS(<span class="hljs-number">1</span>); <span class="hljs-comment">//SS默认高电平</span>
 
}

<span class="hljs-type">void</span> <span class="hljs-title function_">MySPI_Start</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 MySPI_W_SS(<span class="hljs-number">0</span>);				<span class="hljs-comment">//拉低SS，开始时序</span>
}
<span class="hljs-type">void</span> <span class="hljs-title function_">MySPI_Stop</span><span class="hljs-params">(<span class="hljs-type">void</span>)</span>
{
 MySPI_W_SS(<span class="hljs-number">1</span>);				<span class="hljs-comment">//拉高SS，终止时序</span>
}

<span class="hljs-comment">//</span>
<span class="hljs-type">uint8_t</span> <span class="hljs-title function_">MySPI_SwapByte</span><span class="hljs-params">(<span class="hljs-type">uint8_t</span> ByteSend)</span>
{
<span class="hljs-comment">// uint8_t i,ByteReceive=0x00;</span>
<span class="hljs-comment">// for(i=0;i&lt;8;i++)</span>
<span class="hljs-comment">// {</span>
<span class="hljs-comment">//  //移出数据</span>
<span class="hljs-comment">//  MySPI_W_MOSI(ByteSend &amp; (0x80&gt;&gt;i));</span>
<span class="hljs-comment">//  MySPI_W_CLK(1);</span>
<span class="hljs-comment">//  //移入数据</span>
<span class="hljs-comment">//  //当MISO为1时，置变量指定位为1，当MISO为0时，不做处理，指定位为默认的初值0</span>
<span class="hljs-comment">//  if(MySPI_R_MISO()==1){ByteReceive |= (0x80&gt;&gt;i);}</span>
<span class="hljs-comment">//  MySPI_W_CLK(0);</span>
<span class="hljs-comment">// }</span>
<span class="hljs-comment">// return ByteReceive;</span>
 
 <span class="hljs-keyword">while</span> (SPI_I2S_GetFlagStatus(SPI1, SPI_I2S_FLAG_TXE) == RESET);
 SPI_I2S_SendData(SPI1, ByteSend);
 
 <span class="hljs-keyword">while</span> (SPI_I2S_GetFlagStatus(SPI1, SPI_I2S_FLAG_RXNE) == RESET);
 
 <span class="hljs-keyword">return</span> SPI_I2S_ReceiveData(SPI1);
} 
</code></pre>

<h2 id="4-CAN总线"><a href="#4-CAN总线" class="headerlink" title="4. CAN总线"></a>4. CAN总线</h2><blockquote>
<p>STM32–CAN总线</p>
</blockquote>
<p>–静候佳音</p>
]]></content>
      <categories>
        <category>STM32</category>
      </categories>
      <tags>
        <tag>STM32</tag>
        <tag>I2C</tag>
        <tag>SPI</tag>
        <tag>USART</tag>
        <tag>CAN</tag>
      </tags>
  </entry>
</search>
